# Approved UI Redesign Progress

| Lesson name                  | Reference screens found                                      | Shared engine used                                                                                                       | UI completed | All tabs tested                                                   | All controls tested                                                                                                                                                                                         | Datasets tested                                                                                                       | Live training tested                                                                          | Live data editing tested                                                                              | Live inference tested                                                                                                    | Screenshot comparison completed                                       | Errors resolved                                                                                                                                     | Final status |
| ---------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | ------------ | ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| Home Dashboard               | `001__home.png`                                              | Existing 136-route catalog and approved platform dashboard component; inline SVG visual system                            | Yes          | N/A — dashboard sections replace lesson tabs                      | Yes — curriculum/demo/resume, four paths, six category cards, catalog search, utility and sidebar navigation                                                                                                 | Existing route catalog exposed through the complete 136-link catalog                                                  | N/A                                                                                           | N/A                                                                                                   | Yes — progress, streak, paths, category visualizations, SVM continuation and recent experiment states are rendered       | Yes — approved desktop reference plus 391×844 responsive comparison       | Yes — placeholder graphics replaced; navigation actions now open real routes; no overflow, browser warnings, lint, type, test, or build failures | Complete     |
| Documentation                | `002__documentation.png`                                     | Existing documentation route preserved at `?advanced=1`; approved documentation workspace                                | Yes          | Yes — Getting Started, Algorithms, Datasets, Experiments, API     | Yes — table of contents, SDK/CLI/REST choices, copy/playground, guides, search and navigation                                                                                                               | Platform examples and algorithm reference cards                                                                       | N/A                                                                                           | N/A                                                                                                   | Yes — active documentation actions report state                                                                          | Yes — exact 1672×941 plus 390×844 capture                             | Yes — semantic browser, responsive overflow, focused lint and TypeScript audits pass                                                                | Complete     |
| Dataset Library              | `003__dataset-library.png`                                   | Existing dataset library preserved at `?advanced=1`; approved browsing/profile workspace                                 | Yes          | Yes — Browse, Preview, Profile, Versions                          | Yes — search/filter, row selection, dataset actions, import/upload, sidebar/header controls                                                                                                                 | Ten representative datasets plus native CSV/JSON/TSV chooser                                                          | N/A                                                                                           | Yes — dataset selection changes preview and native file input accepts data files                      | Yes — preview, schema, missingness, balance, feature types and insights derive from selected data metadata               | Yes — exact 1536×1024 plus 390×844 capture                            | Yes — upload action and reference header actions added; semantic browser, overflow, lint and TypeScript audits pass                                 | Complete     |
| Sitemap                      | `004__sitemap.png`                                           | Existing navigation catalog preserved at `?advanced=1`; approved curriculum map                                          | Yes          | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — prerequisite toggle, zoom, algorithm nodes, four filters and dataset action                                                                                                                           | Platform curriculum/catalog metadata                                                                                  | N/A                                                                                           | N/A                                                                                                   | Yes — progress totals and node states are represented throughout the map                                                 | Yes — exact 1672×941 plus 390×844 capture                             | Yes — page/inner-grid selector collision found in screenshot QA and fixed; semantic browser and overflow audits pass                                | Complete     |
| Implementation Matrix        | `005__implementation-matrix.png`                             | Existing implementation inventory preserved at `?advanced=1`; approved comparison matrix                                 | Yes          | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — search, paradigm/readiness/dataset/license/browser filters, dataset and sidebar actions                                                                                                               | 15 algorithms and 24-dataset coverage summary                                                                         | N/A                                                                                           | N/A                                                                                                   | Yes — readiness, coverage, training, metrics and compatibility rows are data-driven                                      | Yes — exact 1536×1024 plus 390×844 capture                            | Yes — page/inner-grid selector collision found in screenshot QA and fixed; semantic browser and overflow audits pass                                | Complete     |
| Q-Learning Grid World        | `097__ml__reinforcement-learning__q-learning-grid-world.png` | Shared tabular Q-learning engine with seeded epsilon-greedy training, Bellman updates, learned policy and reward history | Yes          | Yes — Q-Table, Policy, Episode History                            | Yes — wall/penalty/erase tools, 42 grid cells, reset, α/γ/ε/episode ranges, train/clear, save/header/rail controls                                                                                          | Editable deterministic 7×6 Grid World with walls, penalties, start and goal                                           | Yes — seeded 1,000-episode run produces learned nonzero Q-values and 31 visible policy arrows | Yes — native grid cell editing updates obstacles/rewards and invalidates the prior fit                | Yes — Q-table, greedy policy, value shading, reward chart and summary derive from the trained agent                      | Yes — exact 1672×941 plus 390×844 capture                             | Yes — generic shell isolation, responsive containment and deterministic seeded-engine support completed; original lesson preserved at `?advanced=1` | Complete     |
| Simple Linear Regression     | `006__ml__supervised__simple-linear-regression.png`          | `simpleLinearRegression`, shared regression metrics                                                                      | Yes          | N/A — approved screen has no tab strip                            | Yes                                                                                                                                                                                                         | Sample A, Sample B, Low-noise                                                                                         | Yes — Train Model and Animate Fit                                                             | Yes — add, drag, double-click remove, noise regeneration                                              | Yes — line prediction and metrics update from live parameters/data                                                       | Yes — 1672×943 approved viewport                                      | Yes — no console/runtime warnings                                                                                                                   | Complete     |
| Multiple Linear Regression   | `007__ml__supervised__multiple-linear-regression.png`        | `multipleLinearRegression`, shared regression metrics                                                                    | Yes          | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — feature/color selectors, plot layers, coefficients, auto-update, theme, collapse, save/share/report actions                                                                                           | Housing Prices (15), Energy Demand (168), imported CSV (6)                                                            | Yes — housing, energy, and imported CSV retraining                                            | Yes — cell edits, add/remove row, dataset reset, compatible CSV import                                | Yes — housing price and energy-demand predictions respond to inputs and coefficients; auto-update freeze/resume verified | Yes — 1536×1024 approved viewport                                     | Yes — clean runtime console, ESLint, TypeScript, route/navigation audits, and production build                                                      | Complete     |
| Polynomial Regression        | `008__ml__supervised__polynomial-regression.png`             | `polynomialFeatures`, `ridgeRegression`, shared regression metrics                                                       | Yes          | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — degree/ridge/noise controls, scaling, interaction mode, plot editing/reset/focus, theme, quick actions, report action, Home                                                                           | Sine Wave (200), Synthetic Linear (80), Student Marks (120), Energy Demand (168), imported CSV (6)                    | Yes — default and imported CSV retraining                                                     | Yes — table cell edit, chart point insertion, add/remove row, dataset switching/reset, CSV import     | Yes — Explain tab prediction updates from new x against the current fitted model                                         | Yes — 1536×1024 approved viewport plus 1024px/390px responsive checks | Yes — zero runtime console errors; ESLint, TypeScript, route/navigation audits, and production build pass                                           | Complete     |
| Ridge Regression             | `009__ml__supervised__ridge-regression.png`                  | `ridgeRegression`, shared regression metrics                                                                             | Yes          | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — λ numeric/path/L2 controls, intercept, standardization, plot range, dataset controls, bookmark/share/menu, collapse, Home                                                                             | Synthetic Nonlinear (1000×10), Housing (15×5), Energy (168×5), Synthetic Linear (120×3), imported CSV (8×3)           | Yes — default and imported CSV training                                                       | Yes — cell edits, add/remove row, switching, CSV import through Dataset                               | Yes — Explain prediction responds to feature input                                                                       | Yes — 1536×1024 approved viewport plus 1024px/390px responsive checks | Yes — zero runtime console errors; ESLint, TypeScript, route/navigation audits, and production build pass                                           | Complete     |
| Lasso Regression             | `010__ml__supervised__lasso-regression.png`                  | `lassoRegression` coordinate descent, shared regression metrics                                                          | Yes          | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — λ path/slider, CV method, auto-select, standardization, intercept, iteration limit, geometry mode, reset/train, bookmark/share, collapse, Home                                                        | California Housing sample (20,640×8), Housing (15×5), Energy (168×5), Synthetic Sparse (120×4), imported CSV (8×3)    | Yes — auto/manual CV selection and imported CSV training                                      | Yes — cell edits, add/remove row, dataset switching, CSV import                                       | Yes — Explain prediction changed from 3.1944 to 9.2840 with a new live feature value                                     | Yes — 1536×1024 approved viewport plus 1024px/390px responsive checks | Yes — zero runtime console errors; ESLint, core smoke, TypeScript, route/navigation audits, and production build pass                               | Complete     |
| Elastic Net Regression       | `011__ml__supervised__elastic-net-regression.png`            | `elasticNetRegression` mixed L1/L2 coordinate descent, shared regression metrics                                         | Yes          | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — alpha, L1 ratio, Combined/Lasso/Ridge paths, target scaling, per-feature selection, Select Non-Zero, reset/train, share/save/menu, collapse, Home                                                     | California Housing sample (20,640×10), Housing (15×5), Energy (168×5), Synthetic Sparse (180×6), imported CSV (8×4)   | Yes — live mixed-penalty training on default and imported data                                | Yes — cell edits, add/remove row, dataset switching, CSV import, feature inclusion toggles            | Yes — imported-data prediction changed from 2.0276 to 7.5744 with a new income value                                     | Yes — 1536×1024 approved viewport plus 1024px/390px responsive checks | Yes — zero runtime console errors; ESLint, core smoke, TypeScript, route/navigation audits, and production build pass                               | Complete     |
| Decision Tree Regression     | `012__ml__supervised__decision-tree-regression.png`          | `buildRegressionTree`, prediction/depth/leaves/flatten/path utilities, shared regression metrics and CSV parser          | Yes          | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — split thresholds, depth, minimum leaf size, cost-complexity pruning, Manage/Prune, samples, feature/partial-dependence selection, node inspection, reset/update, theme, settings/help, collapse, Home | Bike Sharing Demand (8,760×10), Energy (168×10), Housing (15×5), Synthetic Steps (240×4), parsed CSV fixture (8×4)    | Yes — live CART retraining and completion/progress state                                      | Yes — cell edits, add/remove row, four dataset switches, reusable CSV parser verified against fixture | Yes — prediction changed from 173.62 to 263.96 after a live humidity edit                                                | Yes — 1672×941 approved viewport plus 1024px/390px responsive checks  | Yes — runtime interaction audit, ESLint, core smoke, TypeScript, route/navigation audits, and production build pass                                 | Complete     |
| Random Forest Regression     | `013__ml__supervised__random-forest-regression.png`          | seeded bootstrap CART forest, randomized feature subsets, OOB prediction, importance and distribution utilities          | Yes          | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — trees, depth, minimum leaf size, max features, bootstrap, sample size, random seed/randomize, train/reset, dataset actions, lesson actions, theme/help/guide, collapse, Home                          | California Housing sample (20,640×8), Energy (168×6), Housing (15×5), Synthetic Nonlinear (360×4), imported CSV (8×4) | Yes — 200-tree default and customized 20-tree forest training                                 | Yes — cell edits, add/remove row, four dataset switches, native CSV file-chooser import               | Yes — mean prediction changed from 1.858 to 2.472 after a live MedInc edit                                               | Yes — 1672×941 approved viewport plus 1024px/390px responsive checks  | Yes — zero browser warnings/errors; ESLint, core smoke, TypeScript, route/navigation audits, and production build pass                              | Complete     |
| Gradient Boosting Regression | `014__ml__supervised__gradient-boosting-regression.png`      | stage-wise residual fitting with shared CART weak learners, shrinkage, subsampling, validation, and importance           | Yes          | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — estimators, learning rate, depth, subsampling, early stopping, validation fraction, stage navigation, contribution/tree toggles, reset, dataset/header/lesson actions, theme, collapse                | California Housing sample (8,256×8), Energy (168×6), Housing (15×5), Synthetic Nonlinear (320×4), imported CSV (8×3)  | Yes — default 4-stage and customized 6-stage boosting                                         | Yes — cell edit, add/remove row, four dataset switches, native CSV file-chooser import                | Yes — prediction changed from 6.252 to 8.458 after a live MedInc edit                                                    | Yes — 1672×941 approved viewport plus 1024px/390px responsive checks  | Yes — zero browser warnings/errors; ESLint, core/engine smoke, TypeScript, route/navigation audits, and production build pass                       | Complete     |

| Support Vector Regression | `015__ml__supervised__support-vector-regression.png` | shared standardized ε-insensitive kernel optimizer with Linear, RBF, and Polynomial kernels | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — kernel, C, ε, γ, margins, train/reset, dataset/upload, theme, sidebar, lesson/quick actions, inference | Bike Sharing Demand (17,379 metadata/180 live rows), Energy (168), Housing (15), Synthetic Wave (240), imported CSV (8) | Yes — default RBF and customized 8-row Linear SVR | Yes — cell edit, add/remove row, four dataset switches, native CSV import | Yes — prediction changed from 67.575 to 255.262 after live feature edit | Yes — 1672×941 approved viewport plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; ESLint, core/engine smoke, TypeScript, route/navigation audits, and production build pass | Complete |

| Logistic Regression | `016__ml__supervised__logistic-regression.png` | shared gradient-descent logistic classifier extended backward-compatibly with L2 regularization; shared binary metrics | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — score threshold range/numeric, Probability/Log-Odds/Both, L2 range/numeric, train/reset/resume, dataset/upload, theme, lesson actions, live inference | University Admissions (120), Loan Approval (90), Customer Churn (140), Synthetic Binary (160), imported CSV (10) | Yes — default 120-row and customized imported 10-row training | Yes — feature/class edit, add/remove row, four dataset switches, native CSV import | Yes — admission probability changed from 72.1% to 94.7% after a live score edit | Yes — 1672×943 approved viewport plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; ESLint, core/engine smoke, TypeScript, route/navigation audits, and production build pass | Complete |

| Multinomial Logistic Regression | `017__ml__supervised__multinomial-logistic-regression.png` | genuine shared softmax cross-entropy optimizer with per-class coefficients, probabilities, inference, and L2 | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — x1/x2 projection, PCA selection, L2/C range and numeric controls, four view toggles, point selection, retrain/reset, quick actions, dataset/upload | Iris (150×4), Wine (178×4), Seeds (210×4), Synthetic Blobs (180×4), imported CSV (9×4) | Yes — default 150-row and customized/imported 9-row retraining | Yes — feature/class edit, add/remove row, four dataset switches, native CSV import | Yes — selected-point probabilities changed from 0.24/0.27/0.49 to 0.41/0.23/0.36 | Yes — 1672×941 approved viewport plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; ESLint, core/engine smoke, TypeScript, route/navigation audits, and production build pass | Complete |

| KNN Classification | `018__ml__supervised__knn-classification.png` | existing shared `knnPredict`/`knnClassifyAll` engine with Euclidean, Manhattan, and cosine distance | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — query X/Y and plot click, K stepper/numeric/range, all distance metrics, radius numeric/range, boundary/link layers, train/reset, upload, lesson actions, quick tips | Iris (150), Synthetic Blobs (90), Wine Chemistry (90), Three Moons (90), imported CSV (9) | Yes — KNN sample indexing and leave-one-out validation on live/imported data | Yes — feature/class edit, add/remove row, four dataset switches, native CSV import | Yes — controlled K=1 live label edit changed inference from Setosa to Virginica | Yes — 1672×941 approved viewport plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; ESLint, core/engine invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| Naive Bayes | `019__ml__supervised__naive-bayes.png` | existing shared Gaussian NB engine with learned class priors, per-class means/variances, log-space posterior normalization, and inference | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — four evidence values/ranges/toggles, empirical/uniform priors, three smoothing modes, X/Y axes, region/point layers, train/reset/upload/share, lesson controls | Fisher's Iris (150), Wine Chemistry (150), Gaussian Clusters (150), Diagnostic Measures (150), imported CSV (9) | Yes — live Gaussian parameter refit on built-in and imported data | Yes — four-feature/class edits, add/remove row, four dataset switches, native CSV import | Yes — representative live queries produced Setosa, Versicolor, and Virginica predictions | Yes — 1672×941 approved viewport plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; ESLint, core/Gaussian NB invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| Decision Tree Classification | `020__ml__supervised__decision-tree-classification.png` | existing shared CART classifier with Gini/entropy splits, prediction, depth, live pruning, path, metrics, regions, and importance | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — depth/minimum split/minimum leaf numeric and range controls, Gini/entropy, cost-complexity pruning, region axis, sample/impurity/animation layers, retrain/reset/upload | Iris (150), Wine Classification (150), Wheat Seeds (150), Synthetic Classes (150), imported CSV (9) | Yes — live CART growth/retraining on built-in and imported data | Yes — four-feature/class edits, add/remove row, four dataset switches, native CSV import | Yes — representative live queries produced Setosa, Versicolor, and Virginica paths/predictions | Yes — 1672×941 approved viewport plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; ESLint, core/CART invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| Random Forest Classification | `021__ml__supervised__random-forest-classification.png` | genuine shared seeded multiclass CART forest with bootstrap/OOB sampling, randomized per-split feature subsets, votes, probabilities, importance, and inference | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — 10–500 trees, depth, sqrt/log₂/all feature strategies, bootstrap, minimum split, OOB scoring, feature sample rate, per-tree in-bag/OOB inspection, retrain/reset/upload/docs/tips | Iris (150), Wine Classification (150), Wheat Seeds (150), Synthetic Classes (150), imported CSV (9) | Yes — live 100-tree default, customized, non-bootstrap, and 500-tree endpoint forests | Yes — four-feature/class edits, add/remove row, four dataset switches, native CSV import | Yes — representative live queries produced Setosa, Versicolor, and Virginica predictions and vote distributions | Yes — exact 1672×941 major geometry plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint, core/forest invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| SVM Classification | `022__ml__supervised__svm-classification.png` | genuine shared deterministic SMO kernel classifier with dual coefficients, linear/RBF/polynomial/sigmoid kernels, support vectors, bias, standardization, margin, and inference | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — four kernels, logarithmic C and gamma numeric/range controls, standardization, point selection/reset/fullscreen, retrain/reset, dataset/upload, mark-complete, theme | Moons (400), Linear Separation (240), Gaussian Blobs (240), XOR Classes (240), imported CSV (8) | Yes — live SMO retraining across built-in/imported data and all kernels | Yes — x/y/class edits, add/remove row, four dataset switches, native CSV import | Yes — representative queries produced Class 0 and Class 1 with signed decision scores | Yes — exact 1672×941 major geometry plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint, core/SMO invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| Gradient Boosting Classification | `023__ml__supervised__gradient-boosting-classification.png` | genuine shared stage-wise binary logit boosting with CART residual learners, shrinkage, deterministic subsampling, probabilities, losses, weights, and staged inference | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — stage slider/navigation, learning-rate numeric/range, four depths, subsample numeric/range, weight/boundary layers, full evolution, retrain/reset/upload, header/theme actions | Two Moons (1,000), Concentric Circles (1,000), Gaussian Blobs (1,000), Linear Separation (1,000), imported CSV (8) | Yes — sequential residual fitting on built-in and imported data | Yes — x/y/class edits, add/remove row, four dataset switches, native CSV import | Yes — representative queries produced both classes with live probabilities | Yes — exact 1672×941 major geometry plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint, core/boosting invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| AdaBoost Classification | `024__ml__supervised__adaboost-classification.png` | genuine shared weighted decision-stump AdaBoost with per-round errors, alpha votes, normalized weight updates, staged scores, and inference | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — round count and navigation, learning-rate numeric/range, early stopping, weighted-point/misclassification/boundary/axis controls, run/reset/upload, quick actions, report, theme, and sidebar actions | Spiral (400), Moons (400), Blobs (400), Outliers (400), imported CSV (8) | Yes — sequential weighted stump fitting on built-in and imported data | Yes — x/y/class edits, add/remove row, four dataset switches, native CSV import | Yes — representative queries produced both classes with live ensemble votes | Yes — exact 1672×941 major geometry plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint, core/AdaBoost invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| XGBoost Concept | `025__ml__supervised__xgboost-concept.png` | genuine shared second-order gradient-boosted regression trees with gradients, Hessians, regularized gain/leaf weights, shrinkage, deterministic row/column subsampling, and staged inference | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — objective/metric selectors, learning rate, depth, row and column sampling, minimum child weight, L2/L1, 10–200 trees, train/reset/upload, theme/share, sidebar and lesson actions | California Housing (320×8), Energy Efficiency (168×8), Compact Housing (150×8), Synthetic Nonlinear (280×8), imported CSV (8×3) | Yes — live deterministic second-order boosted-tree fitting on built-in and imported data | Yes — feature/target edits, add/remove row, four dataset switches, native CSV import | Yes — live prediction changed from 3.501 to 0.972 after a query feature edit | Yes — exact 1536×1024 major geometry plus 1024px/390px responsive checks | Yes — zero browser errors; focused ESLint/Prettier, core/XGBoost invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| K-Means Clustering | `026__ml__clustering__k-means.png` | shared seeded K-Means with genuine random/K-Means++ initialization, nearest-centroid assignment, mean updates, inertia, convergence history, and preserved TensorFlow.js MobileNet image-embedding mode | Yes | Yes — 12 assign/update teaching phases plus preserved advanced numeric/image workbench | Yes — K 2–6, initializer comparison, blobs/rings/mixed shapes, speed, random seed/randomize, run/to-end/autoplay/pause, point select/add/remove, centroid drag/reset, theme, docs/help/tools | Synthetic blobs (150), concentric rings (150), mixed geometry (150), plus existing editable/mall/student and bundled/uploaded image datasets in advanced mode | Yes — live deterministic K-Means fitting and phased playback | Yes — plot add/remove and preserved editable numeric/image inputs | Yes — nearest-centroid assignment updates live after data/centroid changes | Yes — exact 1536×1024 major geometry plus 1024px/390px responsive checks | Yes — zero browser errors; focused ESLint/Prettier, core smoke, TypeScript, route/navigation audits, and production build pass | Complete |

| K-Medoids | `027__ml__clustering__k-medoids.png` | genuine shared deterministic PAM-style medoid optimizer with exact global swap evaluation, precomputed distance matrix, actual-row medoids, Euclidean/Manhattan/Chebyshev metrics, cost history, and convergence | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain plus six algorithm phases | Yes — K 2–5, three distance metrics, random/K-Medoids++ initialization, max iterations, auto-step and delay, phase navigation, play/pause, run/apply/randomize/reset, view/scale/distance layers, header/sidebar actions | Mall Customers with outliers (200), Customer Segments (180), Urban Mobility (220), imported CSV (8) | Yes — live deterministic PAM swap optimization on built-in and imported data | Yes — four dataset paths and native CSV import | Yes — assignments, medoids, costs, swap candidates, outlier impact, and all visual links update from the active fit | Yes — exact 1536×1024 major geometry plus 1024px/390px responsive checks | Yes — zero browser errors; focused ESLint/Prettier, core/PAM invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| Hierarchical Clustering | `028__ml__clustering__hierarchical-clustering.png` | genuine shared deterministic agglomerative clustering with single, complete, average, and Ward linkage, Euclidean/Manhattan metrics, full merge history, and height/cluster-count cuts | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain | Yes — four linkage methods, two metrics, numeric/range cut height, optional maximum clusters, merge animation/speed, three built-in datasets, CSV upload, dataset preview, comparison, theme/header/sidebar actions | Iris (150×4), Wine Chemistry (178×4), Wheat Seeds (210×4), imported CSV (8×4) | Yes — complete live hierarchy refit for every data/linkage/metric change | Yes — three generated teaching datasets and native CSV import | Yes — dendrogram, scatter assignments, centroids, cluster summaries, merge log, silhouette and quality metrics all update from the active hierarchy | Yes — approved 1672×941 composition plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint/Prettier, core/agglomerative invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| DBSCAN | `029__ml__clustering__dbscan.png` | shared DBSCAN corrected with explicit visitation, deduplicated density expansion, precomputed ε-neighborhoods, core/border/noise taxonomy, and deterministic labels | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain plus 18 teaching steps | Yes — ε range/numeric, minPts range/numeric, step start/back/play/pause/forward/end, speed, autoplay, three datasets, CSV upload, reset, lesson/header/sidebar/detail actions | Noisy Moons (1,000×2), Gaussian Blobs (1,000×2), Concentric Rings (1,000×2), imported CSV (9×2) | Yes — complete live density refit for every data/ε/minPts change | Yes — three deterministic datasets and native CSV import | Yes — cluster assignments, core/border/noise styling, cluster sizes, status, counts, percentages, and visible playback prefix derive from the active result | Yes — approved 1672×941 composition plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint/Prettier, core smoke, TypeScript, route/navigation audits, and production build pass | Complete |

| Mean Shift | `030__ml__clustering__mean-shift.png` | genuine shared deterministic kernel mode-seeking with Epanechnikov/Gaussian/flat kernels, damped iterative shifts, per-point trajectories, mode merging, convergence history, and inferred cluster count | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare plus iteration timeline | Yes — bandwidth, max iterations, convergence epsilon, three kernels, trajectory/kernel/heatmap/autoplay layers, timeline frames, run/pause/step/reset, three datasets, CSV upload, roadmap/practice/resources | Mixed Peaks (200×2), Multi-density Peaks (200×2), Anisotropic Groups (200×2), imported CSV (9×2) | Yes — complete live mode-seeking refit for every data/kernel/parameter change | Yes — three deterministic datasets and native CSV import | Yes — moving kernels, trajectories, centers, cluster sizes, convergence curve, density landscape, counts, iterations, and status derive from the active result | Yes — approved 1672×941 composition plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint/Prettier, core/Mean Shift invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| Gaussian Mixture Model | `031__ml__clustering__gaussian-mixture-model.png` | genuine shared deterministic full-covariance 2D EM with farthest-point initialization, normalized responsibilities and weights, regularized positive covariances, likelihood history, and iteration snapshots | Yes | Yes — Learn, Visualize, Dataset, Train, Metrics, Compare, Explain plus EM/Model Controls | Yes — EM first/previous/play/pause/next/last/iteration, K 2–6, iterations, tolerance, covariance regularization, projection mode, six datasets, CSV upload, reset, completion/settings/prerequisites | Anisotropic Blobs (800×2), Isotropic Blobs (800×2), Moons (800×2), Spirals (800×2), Banana (800×2), Digits projection (800×2), imported CSV (10×2) | Yes — live full EM refit for every dataset and control change | Yes — six deterministic datasets and native CSV import | Yes — soft-membership opacity, assignments, ellipses, means, weights, covariance table, determinants, likelihood curve, summary and convergence derive from the active iteration/model | Yes — approved 1672×941 composition plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint/Prettier, core/GMM invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| Spectral Clustering | `032__ml__clustering__spectral-clustering.png` | genuine shared deterministic spectral pipeline with k-nearest-neighbor RBF/binary affinity, Euclidean/Manhattan distance, optional graph symmetrization, normalized affinity eigenspace, row normalization, seeded K-Means, and graph-quality metrics | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain plus three synchronized pipeline views | Yes — kernel, sigma, neighbors, metric, symmetrization, K 2–6, Laplacian/solver displays, Nyström display option, seed, point size, edge opacity, weights/color layers, seven datasets, CSV upload, reset/recompute/header/sidebar actions | Two Moons (300×2), Three Spirals (300×2), Swiss Roll (300×2), S-Curve (300×2), Two Circles (300×2), Blobs (300×2), Anisotropic (300×2), imported CSV (9×2) | Yes — complete live graph/eigenspace/refit for every data and computational control change | Yes — seven deterministic datasets and native CSV import | Yes — strongest real affinity edges, reordered affinity matrix, embedding, assignments, cluster sizes, eigenspectrum, normalized cut, modularity, silhouette, and density derive from the active fit | Yes — approved 1672×941 composition plus corrected 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint/Prettier, spectral invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| OPTICS | `033__ml__clustering__optics.png` | genuine shared deterministic OPTICS with pairwise metric distances, ε-neighborhoods, minPts core distances, priority-seed expansion, complete ordering/reachability, and threshold-based DBSCAN-style extraction | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain plus Parameters/Inspector | Yes — minPts, automatic/custom maximum distance, Euclidean/Manhattan metric, epsilon threshold, extraction, cluster highlighting, core/order/log layers, palette, four datasets, CSV upload, reset/recompute/save/share/export/sidebar actions | Aggregation (720×2), Two Moons (500×2), Concentric Rings (500×2), Gaussian Blobs (500×2), imported CSV (9×2) | Yes — complete live OPTICS ordering and extraction for every data and computational control change | Yes — four deterministic datasets and native CSV import | Yes — spatial assignments, real ordering path, log/linear reachability plot, extraction threshold, core/border/noise counts, cluster cards, and quick statistics derive from the active result | Yes — approved 1672×941 composition plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint/Prettier, core/OPTICS invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| Kernel PCA | `035__ml__dimensionality-reduction__kernel-pca.png` | genuine shared deterministic kernel PCA with optional feature normalization, RBF/polynomial/sigmoid/Laplacian/linear Gram matrices, double centering, symmetric power-deflation eigenpairs, sorted spectrum, and normalized component variance | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain plus three-stage nonlinear mapping lesson | Yes — five kernels, gamma, polynomial degree, coefficient, centering, normalization, components 2–12, point size/opacity/color, four datasets, CSV upload, recompute/tour/sidebar/workspace actions | Concentric Rings (240×2), Two Moons (240×2), Interlocking Spirals (240×2), Gaussian Blobs (240×2), imported CSV (8×3) | Yes — complete live Gram/centering/eigendecomposition for every data and computational control change | Yes — four deterministic datasets and native labeled/unlabeled CSV import | Yes — original space, nonlinear teaching surface, kernel matrix, projected coordinates, sorted eigenvalue bars, cumulative curve, and explained-variance donut derive from the active result | Yes — approved 1672×941 composition plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint/Prettier, core/Kernel-PCA invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| t-SNE | `036__ml__dimensionality-reduction__tsne.png` | genuine shared deterministic t-SNE with perplexity-matched Gaussian conditional affinities, symmetric joint probabilities, Student-t low-dimensional similarities, early exaggeration, momentum gradient descent, recentering, snapshots, KL history, and neighborhood-quality scores | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain plus four-stage optimization progress | Yes — perplexity, learning rate, early exaggeration, 100–1,000 iterations, Euclidean/Manhattan distance, PCA/random initialization, animate/pause/reset, four datasets, CSV upload, dataset switch, theme/help/share/sidebar actions | Digit Embeddings (180×8), Fashion Embeddings (180×8), Iris (150×4), Swiss Roll Features (180×8), imported CSV (9×5) | Yes — complete live affinity construction and gradient optimization for every data and parameter change | Yes — four deterministic high-dimensional datasets and native labeled CSV import | Yes — high-dimensional view, animated optimization snapshots, 2D embedding, progress, KL curve/value, trustworthiness, continuity, and legend derive from the active result | Yes — approved 1672×941 composition plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint/Prettier, core/t-SNE invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| UMAP Concept | `037__ml__dimensionality-reduction__umap-concept.png` | genuine shared deterministic UMAP-style manifold optimizer with metric kNN search, locally scaled fuzzy memberships, probabilistic fuzzy union, seeded attractive/negative-sampled repulsive layout updates, and neighborhood/distance quality metrics | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain plus paired fuzzy-graph/manifold views | Yes — neighbors, min_dist, Euclidean/Manhattan/cosine metric, random state, spread, densMAP/angular options, six datasets, CSV upload, reset/recompute, help/theme/explanation/export/sidebar actions | Digit Embeddings (180×8), Fashion Images (180×8), COIL-20 Objects (180×8), 20 Newsgroups (180×8), Single Cell (180×8), Iris (150×4), imported CSV (9×5) | Yes — complete live fuzzy graph and 180-epoch layout refit for every data and computational control change | Yes — six deterministic high-dimensional datasets and native labeled CSV import | Yes — weighted fuzzy edges, low-dimensional coordinates, trustworthiness, continuity, neighborhood hit, distance correlation, diagnostic scatter, class compactness, and embedding metadata derive from the active result | Yes — approved 1672×941 composition plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint/Prettier, core/UMAP invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| Linear Discriminant Analysis | `038__ml__dimensionality-reduction__lda.png` | genuine shared deterministic supervised LDA with optional standardization, empirical/uniform priors, class/overall centering comparison, shrinkage-regularized within scatter, between scatter, generalized eigen direction, 1D projection, and leave-one-out 1-NN evaluation | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain | Yes — feature-pair selection, solver display, shrinkage, priors, standardization, center mode, component display, three datasets, CSV upload, reset/run/docs/share/sidebar actions | Iris catalog-derived teaching set (150×4), Wine Chemistry (180×6), Medical Risk (180×4), imported CSV (9×5) | Yes — complete live scatter/eigen projection refit for every data and discriminant control change | Yes — shared Iris catalog source plus two deterministic datasets and native labeled CSV import | Yes — feature scatter, direction vector, 1D scores, class statistics, scatter separation, histogram, leave-one-out confusion matrix/accuracy, scatter matrices, and eigenvalues derive from the active result | Yes — approved 1672×941 composition plus 1024px/390px responsive checks | Yes — zero browser warnings/errors; focused ESLint/Prettier, core/LDA invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| Autoencoder | `039__ml__dimensionality-reduction__autoencoder.png` | genuine shared TensorFlow.js dense autoencoder with Adam optimization, optional Gaussian corruption, configurable bottleneck/hidden architecture, encoder projection, decoder traversal, reconstruction loss, and parameter accounting | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain | Yes — latent dimension, noise, dense/shallow architecture, learning rate, batch size, epochs, train/stop, three datasets, CSV upload, traversal, preset/header/sidebar actions | Digit Glyphs (200×100), Fashion-like Glyphs (200×100), Symbol Grid (200×100), imported pixel CSV (8×16) | Yes — default 12-epoch, custom noisy shallow 4-epoch, stopped 20-epoch, and imported 4-epoch TensorFlow.js runs | Yes — three deterministic image grids and native square-pixel CSV import with dynamic dimensions | Yes — reconstructions, 2D latent positions, seven-frame decoder traversal, loss history, MSE, PSNR, sparsity, compression, parameters, and model size derive from the trained model | Yes — approved 1672×941 three-rail composition plus responsive tablet/mobile rules and document-overflow audit | Yes — zero browser warnings/errors; TensorFlow shared-layer lifecycle fix, focused ESLint/Prettier, core autoencoder invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| Perceptron | `040__ml__deep-learning__perceptron.png` | genuine shared deterministic online perceptron with caller-supplied initial weights/bias, configurable threshold, strict binary step/sigmoid/tanh decisions, mistake-driven updates, early convergence, and full epoch snapshots | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain | Yes — two weights, bias, threshold, learning rate, max epochs, three activations, update rule, timeline, animate/play/pause, train/reset, dataset switch, CSV upload, header/sidebar/explanation actions | Linearly Separable teaching set (120×2), Overlapping Classes (120×2), Diagonal Margin (120×2), imported CSV (10×2) | Yes — animated five-epoch, immediate eight-epoch, nonconvergent noisy, and converged imported-data runs | Yes — three deterministic datasets and native labeled CSV import | Yes — model flow/net value, decision boundary, predictions, epoch updates, misclassification counts, convergence, confusion matrix, accuracy, precision, recall, F1, and summary derive from active weights | Yes — approved 1672×941 geometry plus responsive two-column/mobile rules and document-overflow audit | Yes — zero browser warnings/errors; strict-binary tanh fix, deterministic engine invariants, focused ESLint/Prettier, TypeScript, route/navigation audits, and production build pass | Complete |

| Multilayer Perceptron | `041__ml__deep-learning__mlp.png` | genuine shared deterministic dense MLP with configurable hidden layers, Xavier initialization, ReLU/tanh/sigmoid hidden activations, sigmoid output, mini-batch SGD/Adam, binary cross-entropy, L2, validation split, and full forward inspection | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain | Yes — hidden-layer count, neurons/layer, three activations, output display, bias, Adam/SGD, learning rate, batch size, epochs, L2, neuron selector, flow controls, three datasets, switch/upload, reset/train, sidebar/header actions | Two Moons (240×2), Concentric Circles (240×2), XOR Quadrants (240×2), imported CSV (10×2) | Yes — default 2-8-6-1 Adam, custom 2-5-4-1 SGD/tanh/no-bias, one-hidden-layer sigmoid, dataset-switch, and imported-data fits | Yes — three deterministic nonlinear datasets and native labeled CSV import | Yes — all network weights/connections, selected-neuron preactivation/output, 480-cell probability field, training/validation curves, predictions, confusion matrix, and metrics derive from the fitted model | Yes — approved 1672×941 geometry plus responsive two-column/mobile rules and document-overflow audit | Yes — zero clean-tab warnings/errors; short-import inspection fix, deterministic MLP invariants, focused ESLint/Prettier, TypeScript, route/navigation audits, and production build pass | Complete |

| Neural Network Playground | `042__ml__deep-learning__nn-playground.png` | shared deterministic MLP engine with configurable validation split, one/two hidden layers, neuron count, three activations, Adam/SGD, mini-batches, bias, noise-regenerated datasets, cross-entropy curves, and forward probability inspection | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain | Yes — features, noise, test split, regeneration, layer/neuron controls, three activations, bias, Adam/SGD, learning rate, batch size, epochs, auto-train, train/stop, add/reset canvas, three datasets, CSV upload, sidebar/header actions | Two Moons (300×2), Concentric Circles (300×2), XOR (300×2), imported CSV (10×2) | Yes — default 2-8-1 Adam, custom 2-10-10-1 SGD/tanh/no-bias, regenerated-noise, and imported 100-epoch fits | Yes — deterministic regenerated datasets and native labeled CSV import | Yes — trained topology/weights, 396-cell probability field, train/validation curves, predictions, confusion matrix, precision/recall/F1, accuracy donut, and 20-bin class distributions derive from the fitted model | Yes — approved 1672×941 geometry plus responsive tablet/mobile rules and document-overflow audit | Yes — zero clean-tab warnings/errors; pending-topology diagram fix, deterministic engine checks inherited from MLP, focused ESLint/Prettier, TypeScript, route/navigation audits, and production build pass | Complete |

| Convolutional Neural Network | `043__ml__deep-learning__cnn.png` | genuine shared TensorFlow.js CNN with two Conv2D/ReLU blocks, two MaxPool stages, flattening, a 128-unit dense layer, softmax classification, Adam training, plus live convolution/ReLU/pooling feature-map utilities | Yes | N/A — approved screen is a single layer-inspection workspace; preserved advanced lesson/lab is reachable from both metrics and inspector actions | Yes — all ten pipeline stages, 18 synchronized kernel inputs, filter bounds, stride, padding, activation, bias, batch norm, receptive-field/grid layers, display selector, epoch choices, train/reset, inspector, sidebar actions, and advanced-lab return | Deterministic 96-image 8×8 horizontal/vertical-bar dataset and held-out sample 101 | Yes — primary 5-epoch 32/64-filter run and preserved advanced 18-epoch run both completed in TensorFlow.js | Yes — both 3×3 kernel editors stay synchronized and recompute every feature-map dimension/value; preserved advanced dataset workbench remains available | Yes — epoch loss/accuracy, probability bars, predicted class, model status, feature maps, and dimensions derive from the active computation/model | Yes — approved dark three-rail composition, trained-state screenshot, fixed content scrolling, and responsive collapse rules | Yes — duplicate React keys and placeholder ten-class probabilities removed; clean fresh-tab training produced zero warnings/errors; focused ESLint/Prettier, TypeScript, route/navigation audits, and production build pass | Complete |

| Convolution Visualizer | `044__ml__deep-learning__convolution-visualizer.png` | shared 2D convolution engine extended backward-compatibly with stride, padding, dilation, and bias; live activation, normalization, receptive-field products, scan history, and image sampling | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain | Yes — previous/play/pause/next/timeline, four kernel sizes, four presets, all stride/padding/dilation endpoints, four activations, bias, normalization, four samples, image upload, reset, lesson/header/sidebar actions, invalid-configuration recovery | Four deterministic 7×7 grayscale matrices and uploaded PNG sampled to a real 7×7 luminance matrix | N/A — this is a direct deterministic convolution visualizer, not a fitted model | Yes — native image upload, canvas luminance sampling, live kernel/preset changes, and every numerical convolution parameter | Yes — receptive field, nine elementwise products, current sum, complete output map, min/max, output dimensions, current position, and scan-order evolution derive from the active convolution | Yes — approved 1672×941 observatory composition, reference viewport geometry, saved audit capture, tablet collapse, and mobile single-column rules | Yes — one-position evolution indexing crash found and fixed; invalid effective kernels now render recovery feedback; clean fresh-tab playback produced zero warnings/errors; convolution/dilation invariants, focused ESLint/Prettier, TypeScript, route/navigation audits, and production build pass | Complete |

| Recurrent Neural Network | `045__ml__deep-learning__rnn.png` | genuine deterministic recurrent forward pass with token embeddings, configurable recurrent/input matrices, tanh/ReLU/sigmoid state updates, softmax next-token prediction, and BPTT gradient propagation; preserved TensorFlow.js SimpleRNN lab | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain | Yes — hidden size, three activations, orthogonal/Xavier/small-random initialization, bias modes, sequence length, playback delay, state noise, state/weight/gradient layers, autoplay/step/reset, three samples, native sequence upload, header/sidebar actions, and advanced-lab return | Three deterministic token sequences and uploaded TXT sequence (10 tokens) | Yes — preserved real TensorFlow.js SimpleRNN completed all 18 epochs at 100% accuracy and 0.0002 loss | Yes — native CSV/TXT/JSON token import updates the active sequence, state count, recurrent trace, prediction chain, and target preview | Yes — hidden states, next-token probabilities, trajectory plot, information decay, and BPTT gradient norms derive from the active sequence and recurrent parameters | Yes — approved 1673×940 three-rail geometry, saved audit capture, 1024×768 no-overflow tablet collapse, and mobile single-column rules | Yes — impossible sequence-length choices and undersized uploads corrected; clean fresh tab produced zero warnings/errors; deterministic orthogonality/state/gradient invariants, focused ESLint/Prettier, TypeScript, route/navigation audits, and production build pass | Complete |

| Long Short-Term Memory | `046__ml__deep-learning__lstm.png` | genuine deterministic LSTM recurrence with configurable gate/candidate activations, peephole contributions, architecture scaling, recurrent/input dropout, per-step gate overrides, and exact cell/hidden-state equations; preserved TensorFlow.js LSTM lab | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain | Yes — step first/previous/next/last, auto-advance, three speeds, four editable gates, units, two gate and two candidate activations, peepholes, one/three-layer endpoints, both dropout endpoints, reset, three samples, preview/upload, header/sidebar actions, and advanced-lab return | Three deterministic numeric sequences and imported ten-value CSV | Yes — preserved real TensorFlow.js LSTM completed all 18 epochs at 100% accuracy, 0.0002 loss, and 99.99% held-out probability | Yes — native CSV/TXT/JSON numeric import resizes the real recurrence and timeline; every selected gate is directly editable | Yes — gate values, cell state, hidden state, current equations, timeline, retention/write/output summaries, and dataset preview derive from active inputs and parameters | Yes — approved 1673×940 three-rail composition, saved audit capture, 1024×768 no-overflow tablet collapse, and mobile single-column rules | Yes — trailing-newline phantom value and fixed nine-column timeline found and fixed; clean fresh tab produced zero warnings/errors; recurrence/determinism/bounds/architecture invariants, focused ESLint/Prettier, TypeScript, route/navigation audits, and production build pass | Complete |

| Gated Recurrent Unit | `047__ml__deep-learning__gru.png` | genuine deterministic GRU recurrence with update/reset gates, candidate activation, hidden interpolation, numeric precision, initial state/bias/weight controls, and a synchronized simple-RNN comparison; preserved TensorFlow.js GRU lab | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain | Yes — reset/step, autoplay, speed, compact/detailed views, timeline selection, initial hidden state, gate bias, weight scale, three activations, float32/float16 precision, sequence length 4–8, source state, three datasets, native upload, export/save/header/sidebar actions, and advanced-lab return | Sentiment, news, and review text collections plus imported five-row labeled CSV | Yes — preserved real TensorFlow.js GRU completed all 18 epochs at 100% accuracy, 0.0001 loss, and 99.99% held-out probability | Yes — native CSV/TXT/JSON text import updates source state, preview rows, tokens, gates, trajectories, and comparison | Yes — token signal, update/reset gates, candidate, hidden state, gate bars, trajectories, retention, and simple-RNN comparison derive from the active recurrence | Yes — approved 1673×940 geometry with main workspace `(209, 75)` to `(1342, 874)`, 304px inspector, saved audit capture, 1024×768 no-overflow collapse, and mobile rules | Yes — clean fresh tab produced zero warnings/errors; exact recurrence/bounds/determinism/control-change invariants, focused ESLint/Prettier, TypeScript, route/navigation audits, and production build pass | Complete |

| Transformer Attention | `048__ml__deep-learning__transformer-attention.png` | genuine deterministic scaled dot-product attention with layer/head-specific Q/K/V projections, temperature-scaled scores, stable softmax, optional causal masking, and weighted contextual outputs | Yes | N/A — approved screen is a focused attention inspector with observatory navigation and sequence switching | Yes — all token queries, six layers, twelve heads plus previous/next, temperature endpoints, causal mask, 121 heatmap cells, reset, three sequences, side navigation, theme/help, and sequence switching | Three deterministic natural-language sequences | N/A — this is a direct attention forward-pass inspector, not a fitted model | Yes — sequence switching changes token count and recomputes every Q/K/V row, score, weight, and output | Yes — selected flow widths/percentages, full attention matrix, Q/K/V slices, and contextual embedding derive from the active head and query | Yes — approved 1673×940 geometry: 153px rail, flow `(171,198)` sized `1043×535`, formula panel to `y=926`, 420px inspector at `x=1234`, saved capture, and 1024×768 no-overflow collapse | Yes — clean fresh tab produced zero warnings/errors; row-normalization, causal-mask, weighted-output, deterministic, layer/head/temperature invariants, focused ESLint/Prettier, TypeScript, route/navigation audits, and production build pass | Complete |

| Multi-Head Attention | `049__ml__deep-learning__multi-head-attention.png` | shared scaled dot-product attention composed into 2/4/8 deterministic heads with independent projections, optional causal masking and deterministic attention dropout, exact aggregate heatmap, concatenated contextual output, and model/head dimensions | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain | Yes — 128–768 model dimension, 2/4/8 heads, all/single-head views, output and attention dropout endpoints, bias, causal mask, five sample rows, native upload, reset/preset/run, docs/share/theme/save, and all sidebar actions | Five simple-sentence rows and imported three-sequence CSV | N/A — this is a direct multi-head forward-pass laboratory | Yes — active dataset row and native CSV/TXT/JSON import recompute token matrices, all heads, aggregate attention, concatenation, and output norm | Yes — every head heatmap, summary, comparison shape, aggregate matrix, concatenation length, head dimension, and output norm derive from active computation | Yes — approved 1673×940 geometry: 227px rail, input `(237,105)` sized `1090×110`, heads `(237,223)` sized `1090×395`, lower diagnostics through `y=842`, 320px inspector, 90px footer, saved capture, and 1024×768 no-overflow collapse | Yes — inspector overlap made the causal toggle unclickable and was fixed; clean fresh tab proves mask state/future zeros with zero warnings/errors; aggregation/normalization/dimension/determinism invariants and production build pass | Complete |

| Backpropagation Visualizer | `050__ml__deep-learning__backpropagation-visualizer.png` | genuine deterministic 3→4→2 network with ReLU/tanh/sigmoid forward pass, MSE output deltas, activation derivatives, exact chain-rule gradients for both weight matrices, and learning-rate updates | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain | Yes — all 12 phases, first/previous/play/next/last, single/auto mode, speed endpoints, three activations, sample navigation, learning-rate endpoints, parameter/detail inspector, reset/save, dataset switch/upload, header/sidebar actions | Eight XOR-style samples and imported four-row numeric CSV | Yes — live forward and backward computation reruns for every sample/control change | Yes — native x1,x2,x3,y1,y2 CSV import replaces samples and recomputes every activation, delta, gradient, update, loss, and diagram value | Yes — network nodes/20 edges, chain factors, selected weight update, gradient matrices, activation bars, and update counts derive from the active computation | Yes — approved 1673×940 geometry: step `(214,151)` sized `1064×71`, network `(214,232)` sized `1064×428`, lower diagnostics to `y=925`, 309px inspector, saved capture, and 1024×768 no-overflow collapse | Yes — selected zero-gradient weight changed to an informative live chain; clean fresh tab has zero warnings/errors; finite-difference gradient, exact update, activation/determinism invariants and production build pass | Complete |

| Few-Shot Learning | `051__ml__deep-learning__few-shot-learning.png` | genuine deterministic prototypical-network episodes with N-way/K-shot support sampling, class means, Euclidean/cosine query distances, nearest-prototype prediction and normalized confidence; preserved TensorFlow.js MobileNet live-image lab | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain | Yes — N-way 2–10, K-shot 1–10, single/five-query modes, both metrics, three embedding choices, boundary/distance layers, new/next/reset episode, five sample-result cards, dataset controls, header/sidebar actions, and advanced-lab return | Deterministic miniImageNet-style class episodes; preserved native image upload/camera support in MobileNet mode | Yes — all prototypes and predictions recompute for every episode/control change; preserved MobileNet performs real pretrained embedding inference | Yes — live-image mode retains native per-class image upload/camera capture; primary episode data regenerates deterministically | Yes — support scatter, prototypes, query links, nearest class, confidence, all distances, summaries, and coordinates derive from active episode | Yes — approved 1673×940 geometry: title `(228,111)` sized `1129×84`, plot `(228,207)` sized `1129×450`, result/prototype row to `y=872`, 290px inspector, saved capture, and 1024×768 no-overflow collapse | Yes — clean fresh tab produced 30 supports/6 prototypes/5 queries with zero warnings/errors; prototype-mean/count/nearest/determinism invariants and production build pass | Complete |

| Network Builder | `052__ml__deep-learning__network-builder.png` | genuine sequential CNN shape/parameter evaluator with input, Conv2D same/valid padding, BatchNorm, pooling, global average pooling, dropout and dense layers; preserved TensorFlow.js dense-network builder/trainer | Yes | Yes — Learn, Visualize, Dataset, Build / Train, Metrics, Compare, Explain | Yes — dataset selection, add/delete/clear/reset layers, dense and convolution editing, same/valid padding, validation, code/preview overlays, zoom, templates/layout, inference, export/share/sidebar actions, and advanced trainer return | CIFAR-10, Fashion-MNIST, Tiny Objects, plus the preserved editable dataset catalog and synthetic TensorFlow training sets | Yes — every architecture edit recomputes tensor shapes, validity and exact parameter totals; preserved TensorFlow.js lab completed a 45-epoch Minimal model run | Yes — dataset input shapes drive all downstream dimensions; preserved advanced lab retains its full editable grid and data-loading workflow | Yes — all layer cards, shape flow, validation state, layer/total parameters and memory derive from the active architecture | Yes — approved 1673×940 three-rail geometry with 210px suite rail, header, 244px dataset rail, central architecture, 280px inspector, saved capture, and 1024×768 no-overflow collapse | Yes — invalid dense-after-image state and reset recovery verified; clean browser diagnostics, shape/parameter/padding invariants, focused ESLint/Prettier, TypeScript, route/navigation audits, and production build pass | Complete |

| Transfer Learning | `053__ml__deep-learning__transfer-learning.png` | genuine deterministic frozen-feature transfer engine with a trained multiclass softmax head, optional trainable feature scales, cross-entropy optimization, top-1/top-5 evaluation, loss history, and parameter accounting | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — three datasets/backbones/optimizers, four unfreezing depths, learning-rate endpoint, batch/epoch/patience inputs, early stopping, fine-tune/reset, native upload, docs, data prep, quick actions, and sidebar actions | Oxford Flowers 102, Stanford Dogs, Food-101, and imported six-row labeled CSV | Yes — 25-epoch default and 12-epoch custom fine-tuning runs completed from the active numerical model | Yes — native CSV/JSON upload reads and reports the imported row count; dataset choices alter classes, split counts, head size, insights, and deterministic training data | Yes — before/after accuracies, top-5 values, loss curves, frozen/trainable parameter split, architecture head, and footer derive from active settings/results | Yes — approved 1673×940 geometry with 230px suite rail, 120px header, 738px architecture/comparison column, 316px controls, 346px dataset rail, saved capture, and 1024×768 no-overflow collapse | Yes — clean fresh-tab diagnostics; decreasing-loss/determinism/finite-history/parameter invariants, focused ESLint/Prettier, TypeScript, route/navigation audits, and production build pass | Complete |

| Train-Test Split | `054__ml__evaluation__train-test-split.png` | shared seeded `trainTestSplit` composed into a genuine deterministic three-way train/validation/test splitter with optional class stratification and class-distribution summaries | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — validation/test ratios, lock, stratification, shuffle/original order, advanced options, reset, dataset selection, native upload, leakage review, linked insights, progress, header/sidebar actions | Titanic Survival v2 (887), Customer Churn (7,043), Iris Species (150), imported passenger CSV (12) | N/A — this lesson performs deterministic dataset partitioning rather than model fitting | Yes — dataset switching and native CSV import update split membership, counts, target distributions, age histograms, quick stats, and labels | N/A — split composition and diagnostics are the direct outputs of the active preprocessing operation | Yes — approved 1673×940 geometry with 205px lesson rail, 136px header, 1,027px split/distribution workspace, 392px controls, saved capture, and 1024×768 no-overflow collapse | Yes — Pclass/Sex cards and age curves corrected to consume their real fields; clean browser diagnostics; disjoint/coverage/count/stratification/determinism invariants, TypeScript, route/navigation audits, and production build pass | Complete |

| Cross Validation | `055__ml__evaluation__cross-validation.png` | genuine deterministic seeded/stratified K-fold engine with per-fold nearest-centroid fitting, held-out predictions, macro precision/recall/F1 and accuracy scoring, plus aggregate variance | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — K=2–10, stratification, shuffle, random seed, four scoring metrics, play/pause rotation, reset, three datasets, native upload, compare and sidebar actions | Iris (150×4), Wine (178×4), Breast Cancer (569×4), imported labeled CSV (12×4) | Yes — every fold independently fits centroids on K-1 partitions and predicts its held-out partition | Yes — dataset selection and native CSV import replace the active samples/features/labels and rerun every fold | Yes — per-fold predictions, score matrix, distribution, bars, mean, variance, standard deviation and insights derive from real held-out results | Yes — approved 1673×940 geometry with 246px rail, 181px header, 1,057px fold matrix, 304px controls, lower three-card diagnostics, saved capture, and 1024×768 no-overflow collapse | Yes — clean browser diagnostics; complete once-only validation coverage, train/validation disjointness, bounded scores, equal fold sizes, determinism, TypeScript, route/navigation audits, and production build pass | Complete |

| Confusion Matrix | `056__ml__evaluation__confusion-matrix.png` | genuine reusable multiclass confusion-matrix evaluator with per-class precision/recall/specificity/F1/support and macro/balanced summaries; preserved binary/manual/loan threshold lab | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — true/predicted filters, counts/%-of-true/normalize views, class focus, 25 linked cells, sample paging, reset, three datasets, switch/upload, download/fullscreen/theme/notifications/sidebar actions, and advanced editor entry | HAM10000-style predictions (2,585), Iris (150), News Topics (963), imported 12-row prediction CSV, preserved Loan Approval data | N/A — this lesson evaluates supplied predictions; preserved loan mode recomputes outcomes from a live decision threshold | Yes — native actual/predicted CSV import replaces labels and matrix; preserved advanced mode retains manual TP/TN/FP/FN editing and threshold controls | Yes — matrix, cell shares, supports, accuracy, balanced accuracy, all per-class/macro metrics, largest-error insight, and linked sample labels derive from active predictions | Yes — approved 1673×940 geometry with 212px rail, 120px header, matrix `(231,134)` sized `913×576`, 502px metric rail, 207px gallery, saved capture, and 1024×768 no-overflow collapse | Yes — original rich lab preserved at `?advanced=1`; clean diagnostics; exact matrix/total/support/accuracy/bounded-metric invariants, focused ESLint/Prettier, TypeScript, route/navigation audits, and production build pass | Complete |

| ROC–AUC | `057__ml__evaluation__roc-auc.png` | shared threshold-swept `rocCurve` engine with trapezoidal AUC, dynamic precision–recall curve, confusion metrics, Youden's J optimization, and score-distribution histograms; preserved rich threshold/comparison lab | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — threshold endpoints/reset, five walkthrough thresholds including best J, ROC/PR toggle, three datasets, three model choices, native upload, advanced-lab entry, and header/sidebar actions | Heart Disease—Cleveland (268), Loan Approval (320), Fraud Detection (360), imported 12-score CSV | N/A — this lesson evaluates continuous classifier scores across thresholds; the preserved advanced lab retains its classifier-comparison workflow | Yes — native actual/score CSV import replaces the active score set and recomputes all curves, distributions, thresholds, and metrics | Yes — ROC/PR paths, AUC, best threshold, current point, confusion counts, accuracy/precision/TPR/FPR, score histograms, and walkthrough values derive from active labels and scores | Yes — approved 1536×1024 composition with 202px rail, 122px header, 987px main workspace, 306px control rail, saved audit capture, and 1024×768 no-overflow collapse | Yes — original rich lab preserved at `?advanced=1`; clean browser diagnostics; endpoint/monotonicity/bounded-AUC/confusion-conservation invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| Precision–Recall Curve | `058__ml__evaluation__precision-recall-curve.png` | shared threshold-swept `precisionRecallCurve` and `binaryMetrics` engines with step-integrated average precision, live confusion metrics, class histograms, iso-F1 contours, and threshold recommendations; preserved original compact PR lab | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — range and numeric threshold endpoints, three computed recommendations, all four smoothing bins, baseline/iso-F1/operating-point/confidence toggles, five trend rows, three datasets, switch/upload, export/share/save/reset, positive-class, and all navigation actions | Credit Card Fraud (25,000×30), Customer Churn (10,000×18), Loan Approval (8,000×14), imported 18-score CSV | N/A — this lesson evaluates ranked classifier scores across every distinct threshold | Yes — native actual/score CSV import replaces active labels and scores and recomputes the curve, histograms, prevalence, suggestions, trends, and metrics | Yes — AP, baseline lift, precision/recall/F1/specificity/FPR/FNR, confusion counts, predicted-positive share, operating point, and suggestions all derive from active labels, scores, and threshold | Yes — approved 1672×941 geometry with 183px rail, 124px header, 1,164px analysis row, 290px controls, full-width 1,444px lower diagnostics, saved capture, 1024×768 internal-scroll collapse, and 390×844 no-overflow mobile audit | Yes — quadratic suggestion computation, invalid recommendation fallback, clipped lower trend, and inaccessible compact scrolling fixed; original lab preserved at `?advanced=1`; clean diagnostics; monotonic-recall/bounded-AP/confusion-conservation invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| Regression Metrics | `059__ml__evaluation__regression-metrics.png` | deterministic real-valued regression datasets with shared MAE/MSE/RMSE/R² engines, residual computation, genuine IQR/σ outlier analysis, linked scatter plots, and preserved original bias/noise laboratory | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — three models and datasets, outlier visibility/rules, zero-error line, equal axes, jitter, linked point selection, five plot tools, model comparison, reset/navigation actions, and native upload | California Housing (8,064×8), Ames Housing (2,930×24), Energy Efficiency (768×8), imported 12-row actual/predicted CSV | N/A — this evaluation lesson scores active prediction pairs; model choices deterministically produce distinct finite predictions | Yes — native actual/predicted CSV import replaces the active observations and recomputes residuals, plots, outliers, sensitivity comparisons, and every metric | Yes — residuals equal actual minus predicted; MAE, MSE, RMSE, R², mean/std/range, outlier counts, sensitivity rows, selection, and insights all derive from active pairs | Yes — approved 1672×941 geometry with 221px rail, 238px header, paired 1,147×412 plots, 1,147×178 sensitivity panel, 238px controls, saved desktop/mobile captures, 1024×768 contained-scroll layout, and 390×844 exact-width mobile audit | Yes — checkbox sizing, responsive tab overflow, performance calibration, and 5σ comparison semantics fixed; original lab preserved at `?advanced=1`; clean diagnostics; metric-identity/perfect-prediction invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| Bias–Variance Tradeoff | `060__ml__evaluation__bias-variance-tradeoff.png` | deterministic seeded train/test polynomial and ridge fitting, 15-degree error sweep, bootstrap prediction uncertainty, real MSE/R² metrics, and preserved original compact polynomial-regression lab | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — degrees 1–15, animation, three model kinds and datasets, scaling, noise/test-size endpoints, seed, completion, dataset cycling, reset, collapse, upload, and all navigation actions | California Housing (20,640×8), Ames Housing (2,930×24), Energy Efficiency (768×8), imported 16-row x/y CSV | Yes — each active degree/model fits a ridge-stabilized polynomial or spline basis on the seeded training partition; bootstrap resamples independently refit the active model | Yes — built-in selection regenerates active observations; native x/y CSV import normalizes and replaces them before splitting, fitting, bootstrapping, and evaluation | Yes — train/validation MSE, gap, validation R², fitted curve, complexity sweep, optimal range, and average bootstrap spread derive from the active points and controls | Yes — approved 1536×1024 composition with 202px rail, 147px header, explorer at `(214,203)` sized `1032×641`, 278px control rail, saved desktop/mobile captures, 1024×768 contained-scroll layout, and 390×844 exact-width mobile audit | Yes — hidden final tabs, conceptual calibration, U-shaped high-degree behavior, and breakpoint containment verified; original lab preserved at `?advanced=1`; clean diagnostics; deterministic finite endpoint checks, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| Missing Values | `061__ml__preprocessing__missing-values.png` | deterministic representative datasets with exact cell/column missingness accounting, numeric mean/median/KNN/MICE/constant previews, binary-indicator state, mean-clean export, and preserved full imputation workbench | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — table/compact views, missing-only filter, column search, 10/25/50 paging, three datasets, indicators, heatmap/settings, method comparison/configuration, export, upload, continue, and navigation actions | California Housing (20,640×10), Ames Housing (2,930×10), Titanic Passengers (891×10), imported 12×5 mixed CSV | Yes — numeric fill strategies compute from each active column; preserved workbench applies drop/mean/median/mode/custom operations to active rows | Yes — native CSV parsing replaces columns and rows, recomputes missingness and imputation previews, and feeds clean export | Yes — cell totals, missing/present shares, per-column counts/rates, heatmap, insights, preview values, filters, and exported fills derive from active row cells | Yes — approved 1672×941 composition with 198px rail, 113px header plus 37px tabs, exact `(212,160)` 1,066×382 table, 1,066×240 lower row, 386px analysis rail, saved desktop/mobile captures, 1024×768 contained-scroll layout, and 390×844 exact-width audit | Yes — 37px vertical offset, clipped insights, progress styling, long percentage display, and over-wide indicator columns fixed; original lab preserved at `?advanced=1`; clean diagnostics; zero-blank 75×10 clean-export check, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| Scaling & Normalization | `062__ml__preprocessing__scaling-normalization.png` | real Min-Max, Z-score, and robust transforms using shared mean, median, quantile, and standard-deviation utilities; synchronized distributions/scatters and preserved original comparison workbench | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — all three methods, all/custom feature selection, search and feature toggles, density/scatter/link/log options, reset, plot expansion, dataset/header/sidebar/footer actions, and native upload | Wine Quality White (4,898×11), Breast Cancer Wisconsin (569×11), Diabetes Progression (442×11), imported 12×4 numeric CSV | N/A — this lesson computes deterministic feature transforms rather than fitting a predictive model | Yes — native numeric CSV parsing replaces rows/features and recomputes every transformed series, distribution, scatter, and summary | Yes — raw/scaled paths, scatter coordinates, means, standard deviations, feature labels, and insights derive from the active values and method | Yes — approved 1672×941 three-rail composition with 270px navigation, 143px header, 1,128px main workspace, 274px controls, 55px footer, saved desktop/mobile captures, and 1024×768 no-overflow collapse | Yes — raw/scaled chart domains and scatter method synchronization corrected; original lab preserved at `?advanced=1`; clean diagnostics; Min-Max/Z-score/robust/constant invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| Categorical Encoding | `063__ml__preprocessing__categorical-encoding.png` | reusable one-hot, ordinal, frequency, and leakage-safe K-fold target encoders with feature cardinality analysis, AUC comparison, and preserved original local-row workbench | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — four encoding methods, target column, K-fold/smoothing/minimum-sample/train-split endpoints, transformation/reset, three output cards, export, docs/theme/explorer/footer/sidebar actions, and native upload | Telco Customer Churn (7,043×21), Bank Marketing (45,211×17), Employee Attrition (1,470×35), imported 12×5 categorical CSV | Yes — every transformation is recomputed from the active rows; K-fold target encoding calculates category statistics exclusively from out-of-fold training rows | Yes — native categorical CSV parsing replaces rows/columns/target and recomputes raw previews, encodings, cardinality, leakage checks, AUC, and export | Yes — encoded matrices, shapes, target values, category risks, out-of-fold scores, positive rate, impact bars, and recommendations derive from active data/settings | Yes — approved 1672×941 composition with 181px suite rail, 214px lesson rail, 126px header, 952px central workspace, 327px controls, 62px footer, saved desktop/mobile captures, 1024×768 no-overflow collapse, and exact-width 390px audit | Yes — full-width raw table, built-in metadata, ID cardinality inclusion, and fold-global leakage were corrected; original lab preserved at `?advanced=1`; clean diagnostics; determinism/bounds/one-hot/own-fold invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| Outlier Detection | `064__ml__preprocessing__outlier-detection.png` | reusable IQR/Z-score classifier with mild/severe bands and IQR winsorization; synchronized scatter, boxplot, density, counts, impact, and preserved original compact statistical lab | Yes | Yes — Learn, Visualize, Dataset, Transform, Metrics, Compare, Explain | Yes — IQR/Z-score, seven feature choices, multiplier endpoints/steppers, inspect/remove/cap/reset, four scatter tools, create-version toggle, transform/header/sidebar actions, and native upload | Seattle Air Quality (4,356×12), NYC Sensor Network (8,760×10), Delhi Air Monitor (5,256×14), imported 12×4 numeric CSV | Yes — detection and transformations recompute from every active numeric observation; remove filters flagged rows and cap winsorizes at computed IQR fences | Yes — native numeric CSV import replaces rows/features and recomputes every plot, fence, result band, extreme-point table, and impact measure | Yes — method scores, mild/severe labels, quartiles/fences, scatter colors, histograms, counts/shares, transformed rows, and mean/std impact derive from active values/settings | Yes — approved 1672×941 geometry with 196px rail, 73px header, 90px objective/progress strip, workbench at `(207,163)` sized `1194×734`, 245px controls at `x=1411`, 44px footer, saved desktop/mobile captures, 1024×768 no-overflow collapse, and exact-width 390px audit | Yes — metadata-scaled result counts, action/footer row state, slow lazy-load capture, and responsive containment resolved; original lab preserved at `?advanced=1`; clean diagnostics; band/bounds/capping/determinism invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| Feature Selection | `065__ml__preprocessing__feature-selection.png` | reusable variance, Pearson/point-biserial correlation, and model-importance ranking engine with synchronized feature toggles, correlation network, performance curves, and preserved original housing top-k lab | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — three ranking modes, individual and bulk selection, manual/automatic/threshold modes, network tools, reset/save, four insight cards, activity/header/sidebar actions, collapse/restore, and native upload | Telco Customer Churn (7,043×20), Loan Approval Risk (4,269×20), Medical Risk Cohort (3,200×20), imported 12×7 numeric CSV | Yes — all ranks, pairwise network edges, selected-feature metrics, and performance curves recompute from active observations and target | Yes — native numeric CSV import replaces rows/features/target and recomputes ranking, selected counts, correlations, network, metrics, and curves | Yes — variance/correlation/importance values, sorted ranks, network edges, toggle states, AUC/log-loss summaries, and recommendations derive from active rows and selection | Yes — approved 1536×1024 geometry with 206px rail, 72px header, 49px tabs, 83px summary, ranking at `(220,204)` sized `390×578`, network at `(619,204)` sized `515×578`, controls at `x=1144`, saved desktop/mobile captures, 1024×768 contained-scroll layout, and exact-width 390px audit | Yes — alternate-dataset target leakage, global `.collapse` class collision, toggle alignment, zero-selection marker, selector/ranking synchronization, and responsive clipping corrected; original lab preserved at `?advanced=1`; clean diagnostics; correlation/ranking/performance invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| Polynomial Features | `066__ml__preprocessing__polynomial-features.png` | reusable multivariate monomial generator, optional bias/interactions, post-transform standardization, and ridge-stabilized least-squares evaluator with synchronized matrix, equations, plots, metrics, and preserved original one-feature lab | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — degrees 1–5, feature add/remove, bias/interactions, scaling, three models, plot/example selectors, result inspection, three datasets, header/sidebar/collapse actions, and native upload | California Housing sample (20,640×4), Energy Efficiency (768×4), Concrete Strength (1,030×4), imported 12×3 numeric CSV | Yes — each degree fits real least-squares predictions against the active expanded design matrix; R², RMSE, MAE, weights, and comparison plots recompute live | Yes — native numeric CSV import treats the final column as target, replaces the active rows/features, and recomputes terms, matrix, equations, fits, plots, and metrics | Yes — monomial names/powers, standardized matrix cells, equation terms, scatter coordinates, degree table, feature counts, and insights derive from active data/settings | Yes — approved 1673×940 geometry with 238px rail, 155px header, input at `(260,155)` sized `207×335`, expansion at `(483,155)` sized `344×335`, matrix at `(843,155)` sized `498×335`, 843px comparison and 225px example at `y=503`, 300px settings at `x=1352`, saved desktop/mobile captures, 1024×768 contained-scroll layout, and exact-width 390px audit | Yes — lower-workspace span, right-rail alignment, degree-slider input event, comparison row density, and mobile header overlap corrected; original lab preserved at `?advanced=1`; clean diagnostics; term-count/shape/standardization/fit/determinism invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| Moving Average | `067__ml__time-series__moving-average.png` | reusable fixed-width trailing/centered moving average, signed lag shifting, residual and absolute-residual analysis with synchronized multiseries charts, controls, insights, and preserved original monthly-sales lab | Yes | Yes — Learn, Visualize, Dataset, Transform, Metrics, Compare, Explain | Yes — windows 1/24/168, trailing/centered alignment, lag −48/0/48, five time ranges, four display layers, five themes, three datasets, reset/export/save/calendar/expand/sidebar/collapse actions, and native upload | Metro Energy Load (2,160 hourly rows), Retail Demand (1,488), Server Traffic (4,320), imported 16-point hourly CSV | N/A — this time-series lesson computes deterministic rolling transforms and residual diagnostics rather than fitting a trainable predictor | Yes — native time/value CSV import replaces the active ordered series and recomputes moving average, lag, residuals, absolute residuals, paths, and footer metadata | Yes — all four chart paths, window overlay, expected lag, residual identity, window-effect indicator, counts, and insight copy derive from active observations/settings | Yes — approved 1536×1024 geometry with 253px rail, 230px lesson header/tabs, toolbar through `y=291`, 987px chart column at `x=267`, 282px controls at `x=1254`, 147px insight row at `y=808`, 46px footer at `y=967`, saved desktop/mobile captures, 1024×768 contained-scroll layout, and exact-width 390px audit | Yes — global `absolute`/`collapse` class collisions, sidebar grid placement, and centered edge-window semantics corrected; original lab preserved at `?advanced=1`; clean diagnostics; trailing/centered/lag/residual/absolute/determinism invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| Exponential Smoothing | `068__ml__time-series__exponential-smoothing.png` | reusable simple exponential smoothing recurrence with one-step forecasts, h-step forecast intervals, residual metrics, and synchronized actual/level/error visualizations plus preserved original monthly-sales lab | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — alpha 0.01/0.28/0.99, horizon 1/12/24, 90/95/99% confidence, animation/error toggles, control collapse, three datasets and cycling, export/theme/learn/sidebar/collapse actions, and native upload | Daily Airline Passengers (1,442 periods), Retail Sales Index (520), Cloud Request Volume (2,160), imported 16-observation CSV | Yes — each alpha recomputes the exact recursive level and one-step forecast; horizon/confidence recompute constant SES forecasts and widening prediction intervals | Yes — native time/value CSV import replaces the ordered observations and recomputes levels, forecasts, bands, errors, metrics, recent-update table, bars, and metadata | Yes — actual/level/one-step/future paths, confidence polygon, errors, level update, MAE/RMSE/MAPE/bias/Theil's U, table, bars, and formula states derive from active observations/settings | Yes — approved 1673×940 geometry with 198px rail, 193px lesson header/tabs, forecast/error panel at `(222,193)` sized `1098×527`, 322px controls at `(1333,193)`, lower diagnostics from `(222,752)` through `y=923`, saved desktop/mobile captures, 1024×768 contained-scroll layout, and exact-width 390px audit | Yes — numeric select-value mismatch, chart/control split, and responsive auto-track compression corrected; original lab preserved at `?advanced=1`; clean diagnostics; recurrence/one-step/horizon/symmetry/widening/bounds/determinism invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| Holt–Winters | `069__ml__time-series__holt-winters.png` | reusable additive/multiplicative triple exponential smoothing engine with level, trend, seasonal, fitted, forecast, residual, and accuracy outputs; synchronized four-panel decomposition plus preserved original time-series lab | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — additive/multiplicative modes, seasonal-length stepper, alpha/beta/gamma endpoints, auto-optimize, forecast horizon minimum/default, five zoom ranges, forecast/calendar actions, three datasets and cycling, header/sidebar/collapse actions, and native upload | Retail Sales daily series, Electricity Load hourly series, Web Traffic daily series, imported 28-observation CSV | Yes — every parameter, season length, mode, horizon, and dataset change reruns genuine Holt–Winters recurrence and recomputes historical fit plus future values | Yes — native date/value CSV import replaces the ordered observations and recomputes all decomposition paths, forecasts, residuals, metrics, summaries, and metadata | Yes — observed/fitted, level, trend, seasonal, residual and forecast paths, MAE/RMSE/MAPE/sMAPE, insight copy, range state, and summary bounds derive from active computation | Yes — approved 1536×1004 geometry with 240px rail, 279px header/progress region, 983px decomposition panel at `(256,279)`, 285px controls at `(1251,279)`, four lower cards from `y=763`, saved desktop/mobile captures, 1024×768 contained-scroll layout, and exact-width 390px audit | Yes — original lab preserved at `?advanced=1`; clean diagnostics; array-shape/forecast-length/finite-metric/seasonality/determinism invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| ARIMA Concept | `070__ml__time-series__arima-concept.png` | reusable differencing, ACF, Durbin–Levinson PACF, ridge-stabilized AR/MA regression, iterative residual fitting, forecast restoration, seasonal forecast offsets, confidence intervals, likelihood, AIC, and BIC; preserved original browser-side ARIMA lesson | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — p/d/q and P/D/Q/S endpoints, d=0/1/2 comparison, forecast horizon 1/24/60, 90/95/99% intervals, run/auto-suggest, inspect/expand, three datasets, upload, every navigation/header action, and sidebar collapse | Canonical 144-month AirPassengers series, 180-day electricity demand, 156-week retail orders, imported 18-observation passenger CSV | Yes — each active order fits a real autoregressive/moving-residual system on the differenced series; forecasts, uncertainty, residuals, likelihood, AIC, and BIC recompute from its fitted coefficients | Yes — native date/value CSV import replaces the ordered series and recomputes decomposition, correlations, regression fit, forecasts, intervals, diagnostics, criteria, and metadata | Yes — ten SVG plots, 148 live ACF/PACF bars, decomposition, forecast bands, residuals, stationarity/order insights, and current-model statistics derive from active data and controls | Yes — approved 1672×941 geometry with 180px observatory rail, 128px header/tabs, 1,160px analysis workspace, 332px model rail, saved desktop/mobile captures, exact reference viewport containment, 1024×768 internal-scroll layout, and exact-width 390px audit | Yes — unstable second-difference forecast composition corrected by separating regular differencing from seasonal residual offsets; original lab preserved at `?advanced=1`; clean diagnostics; differencing/ACF/PACF/shape/interval/order-effect/determinism invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| Time-Series Anomaly Detection | `071__ml__time-series__anomaly-detection.png` | reusable centered rolling-median and MAD detector with robust sigma, adaptive upper/lower bounds, contextual-change scoring, severity classes, synchronized selected-alert detail, and preserved original rolling z-score lab | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — three windows, live pause/resume, three timezones, five chart tools, three models, sensitivity 1.0/2.5/4.0σ, three cooldowns, contextual/expected toggles, marker selection, recalculate, lesson/alert/explain actions, header/sidebar/collapse actions, and native upload | 180-point Server CPU, API latency, and payment-throughput streams plus imported 20-point CPU CSV | Yes — every series/window/sensitivity/context setting reruns robust local-baseline detection and recomputes anomaly score, adaptive bounds, severity, summaries, timeline, alerts, and selected detail | Yes — native time/value CSV import replaces the stream and recomputes all points, bands, markers, alert counts, timeline, breakdown, and tooltip values | Yes — actual/expected paths, threshold polygon, 19 default markers, tooltip, threshold readouts, summaries, alert list, timeline, donut, and insights derive from active detection results | Yes — approved 1536×1024 geometry with 214px observatory rail, 70px data header, 128px lesson band, 1,074px monitoring workspace, 248px controls, 68px status footer, saved desktop/mobile captures, 1024×768 internal-scroll layout, and exact-width 390px audit | Yes — edge-window false positives removed and compact switch spacing corrected; original lab preserved at `?advanced=1`; clean diagnostics; length/finite/band-order/spike/severity/sensitivity-monotonicity/determinism invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| RNN Forecasting | `072__ml__time-series__rnn-forecasting.png` | deterministic recurrent state engine with distinct SimpleRNN/LSTM/GRU transitions, recursive seasonal forecasting, uncertainty bounds, hidden-state trace, training curves, and preserved TensorFlow.js SimpleRNN trainer | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — lookback/horizon/step endpoints, three targets/scalers/model variants/datasets, generate, architecture, four quick actions, upload, all navigation/header actions, and collapse/restore | Electricity Load, Metro Traffic, Solar Generation, plus imported 32-observation hourly load CSV | Yes — settings rerun a normalized recurrent forward pass and recursive forecast; the preserved advanced route executes the browser TensorFlow.js SimpleRNN training pipeline | Yes — native time/value CSV import replaces the active sequence and recomputes recurrent states, forecasts, bands, diagnostics, and row metadata | Yes — input/hidden/output flow, rolling forecast and interval, 64-unit state trace, loss/error curves, summary, and insights derive from the active series and controls | Yes — approved 1536×1024 three-column geometry with 208px observatory rail, 151px header, 1,064px lesson workspace, 244px forecast rail, saved desktop/mobile captures, 1024×768 contained responsive layout, and exact-width 390px audit | Yes — clipped upload/more and forecast action controls, bottom-row alignment, and step-size semantics corrected; original trainer preserved at `?advanced=1`; clean diagnostics; shape/finite/band-order/kind/input-sensitivity/determinism invariants, focused ESLint, TypeScript, route/navigation audits, and production build pass | Complete |

| LSTM Forecasting | `073__ml__time-series__lstm-forecasting.png` | genuine deterministic forget/input/output gates, candidate and cell-state recurrence, recursive multi-step forecast, uncertainty bands, and preserved TensorFlow.js LSTM trainer | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — input/horizon/step and dropout endpoints, three layers/unit/rate/view choices, three datasets, gate/cell/uncertainty toggles, run, export, upload, all navigation actions, and collapse/restore | Electricity Load Australia, Metro Traffic, Solar Power, plus imported 32-observation load CSV | Yes — every series/window/unit choice recomputes actual LSTM gate and cell transitions; preserved advanced mode completed TensorFlow.js training and recursive inference | Yes — native time/value CSV import replaces the active sequence and recomputes states, gates, forecasts, uncertainty, summaries, and labels | Yes — sequence/cell/forecast flow, four gate rings, cell relevance, hidden trace, uncertainty and insights derive from active computation | Yes — approved 1672×941 geometry with 214px lesson rail, 189px objective/dataset header, 1,096px gated workspace, 330px controls, exact-size desktop/mobile captures, and no-overflow 1024×768/390×844 layouts | Yes — control density and visual toggle linkage corrected; original trainer preserved at `?advanced=1`; completed browser training (loss 0.6445, fitted/forecast values), clean diagnostics, gate/state/shape/bounds/determinism invariants, focused ESLint, TypeScript, audits, and build pass | Complete |

| GRU Forecasting | `074__ml__time-series__gru-forecasting.png` | genuine deterministic update/reset gates and candidate-state recurrence, rolling forecasts and uncertainty, gate heatmaps, mechanics diagnostics, and preserved TensorFlow.js GRU trainer | Yes | Yes — Learn, Visualize, Dataset, Transform, Train, Metrics, Compare, Explain | Yes — three datasets/runs/lookbacks/horizons, roll step, layers/units/dropout/batch/epochs/patience endpoints, activation/optimizer/rate/loss/scaling options, early stopping, train/reset, upload, sidebar/header actions, and collapse/restore | Electricity Transformer Load, Traffic Sensor Volume, Wind Farm Output, plus imported 32-observation transformer CSV | Yes — active settings recompute GRU hidden states and forecasts; preserved advanced mode completed TensorFlow.js training and recursive inference | Yes — native time/value CSV import replaces the active series and recomputes state/gate histories, heatmaps, chart, forecast, uncertainty, and metadata | Yes — observed/forecast paths, 360 gate cells, three live gate mechanics, progress, metrics, comparison, and 32-bin error display consume active results/settings | Yes — approved 1672×941 experiment console with 218px navigation, 156px title/toolbar, 1,126px six-panel workspace, 306px configuration rail, exact-size desktop/mobile captures, and no-overflow 1024×768/390×844 layouts | Yes — right-rail density and early-stopping/patience alignment corrected; original trainer preserved at `?advanced=1`; completed browser training (loss 0.2865, fitted MAE 2.88, next 131.27), clean diagnostics, gate/state/finite/bounds/kind/determinism invariants, focused ESLint, TypeScript, audits, and build pass | Complete |

## Verification notes

- Home navigation is available through the AlgoViz brand.
- The focused lesson layout is scoped to this route; other routes retain their existing shell.
- Dataset selection, slope, intercept, noise, residuals, confidence band, equation visibility, training, animation, reset, lesson progress, and challenge navigation were exercised.
- Model values are calculated from the current live points through the existing ordinary least-squares engine. Metrics are calculated through the existing shared metrics utilities.
- Responsive layouts were implemented for desktop, tablet, and narrow mobile widths.
- Multiple Linear Regression preserves the shared normal-equation engine and computes coefficients, predictions, residuals, intervals, and metrics from the current live dataset.
- Its approved desktop composition was verified at 1536×1024 with the three diagnostic cards aligned beneath the regression plot, Prediction Inspector at right, and Insights fully visible without document scrolling.
- Browser interaction covered all seven tabs, layer toggles, feature selections, coefficient overrides/reset, both built-in datasets, editable rows, CSV import, retraining, live inference, saved view, sharing feedback, theme, and sidebar collapse.
- Polynomial Regression uses the shared polynomial expansion and ridge solver for every displayed curve, coefficient, residual, error comparison, confidence band, metric, and prediction; no visual result is hard-coded.
- Its seven-tab state flow was exercised with every built-in source and a two-column CSV, including training on the imported six-row set and retaining that set across tab changes.
- The reference composition was verified at 1536×1024 with no page scrolling, and responsive layouts were verified at 1024×768 and 390×844 without horizontal overflow.
- Ridge Regression retains the shared ridge solver and computes its coefficient paths, fitted curve, error metrics, regularization decomposition, and inference from the active dataset and controls.
- All seven Ridge tabs were exercised across four built-in datasets and an imported eight-row CSV; edits, row operations, training, model comparison, and inference remained live across tab changes.
- Its three-column desktop composition was verified against the approved 1536×1024 reference, with tablet and mobile layouts checked at 1024×768 and 390×844 without horizontal overflow.
- Lasso Regression extends the existing shared coordinate-descent solver with backward-compatible standardization and intercept options; all coefficient paths, fold losses, sparsity states, metrics, comparisons, and predictions are calculated from live model fits.
- The approved Lasso geometry was matched at 1536×1024: 415px upper cards, 234px validation row, 190px insight row, 550px controls, and 299px dataset panel. The document has no horizontal overflow at the verified tablet and mobile widths.
- Browser interaction covered all seven Lasso tabs, four built-in datasets, CSV import, editable rows, add/remove, model-option effects, Min-MSE/1-SE/manual selection, training status, live inference, geometry mode, reset, sharing, bookmarking, and sidebar collapse.
- Elastic Net Regression uses the new shared mixed-penalty coordinate-descent fit for coefficient paths, feature selection, three model-comparison curves, all metrics, and inference. Its incremental fitted-value cache keeps the same objective while making parameter changes responsive.
- The approved Elastic Net composition was matched at 1536×1024 with an 188px sidebar, 875/423px content split, 375px path panel, 241px comparison panel, 180px insight strip, and 229/349/217px right-rail cards.
- Browser coverage included all seven Elastic Net tabs, four built-in datasets, CSV import, editing and row operations, alpha and mixing effects, path modes, target scaling, feature toggles, non-zero selection, training, comparison, inference, reset, share/save/menu actions, collapse, and responsive layouts without horizontal document overflow.
- Decision Tree Regression uses a genuine CART variance-reduction engine with mean-valued leaves, depth/minimum-leaf constraints, preferred lesson splits, and cost-complexity pruning. Tree nodes, piecewise predictions, metrics, inspection details, and inference all derive from the active fitted model.
- Its approved 1672×941 composition was matched with the 248px lesson rail, 1098px dataset/workspace area, 273px control rail, full three-card diagnostic row, and a completely visible Update Tree action. Tablet and 390px mobile views have no document-level horizontal overflow.
- Browser coverage exercised all seven tabs, all four built-in datasets, editable cells, add/remove, every numeric model control, pruning mode, feature and node selection, training, header/sidebar controls, and live inference. The shared CSV parser was separately executed against the eight-row import fixture during that lesson's audit.
- Random Forest Regression replaces the former shallow-stump demo with a reusable seeded regression-forest engine. Every tree is trained on a real sampled dataset; ensemble means, per-tree uncertainty, feature importance, test metrics, tree diversity, and OOB metrics are derived from live predictions.
- Its 1672×941 reference geometry was matched exactly at the major boundaries: 70px objective header, main lesson card at `(296, 187)` sized `1054×490`, diagnostics at `(296, 687)` sized `1054×217`, and settings rail beginning at `x=1360`.
- Browser coverage exercised all seven tabs, every model and sampling control, 200-tree and customized 20-tree training, four built-in datasets, editable cells, add/remove, native CSV import, lesson actions, theme/help/guide, sidebar collapse, reset, and live inference. Browser diagnostics remained free of warnings and errors, and tablet/mobile views had no document-level horizontal overflow.
- Gradient Boosting Regression now fits genuine stage-wise residual trees through the shared CART implementation. Its baseline, residuals, shrinkage updates, stage predictions, train/validation losses, early-stopping behavior, feature importance, metrics, and inference all derive from the active data and controls.
- Its reference composition was matched at 1672×941: the 266px lesson rail, 1071px visualization workspace, 335px controls rail, and the sequential-tree, residual, additive-build-up, and summary boundaries align to the approved screen. Tablet and 390px mobile layouts have no document-level horizontal overflow.
- Browser coverage exercised every tab and model setting, stage navigation, contribution highlighting, tree visibility, four built-in datasets, editable rows, add/remove, native CSV import, default and customized training, header/sidebar actions, summary expansion, reset, and live inference. The browser console remained free of warnings and errors.
- Support Vector Regression replaces the prior kernel-like weighted average with a reusable standardized kernel optimizer for the ε-insensitive objective. The fitted curve, tube boundaries, residual classifications, support/boundary vectors, loss history, metrics, and inference all derive from the active model.
- Its 1672×941 reference composition was matched with the 268px lesson rail, 116px title area, seven-tab strip, large ε-tube plot, three lower diagnostics, 333px control rail, and bottom dataset bar. Tablet and 390px mobile views have no document-level horizontal overflow.
- Browser coverage exercised all seven tabs, three kernels, both numeric and range forms of C/ε/γ, margin visibility, default and customized training, four built-in datasets, editing, add/remove, native CSV import, live inference, reset, theme, outline actions, and sidebar collapse. Browser diagnostics remained free of warnings and errors.
- Logistic Regression preserves the shared gradient-descent classifier and adds backward-compatible L2 regularization. The sigmoid curve, probability and log-odds views, score threshold, confusion matrix, accuracy/precision/recall/F1, distribution summary, odds, and inference all derive from the active fitted model and data.
- Its 1672×943 reference geometry was matched at the major boundaries: 270px lesson navigation, 96px header, 1008×44 tab strip, 1007×510 visualization, 1007×216 metric row, 330px controls rail, and bottom dataset bar. Tablet and 390px mobile views have no document-level horizontal overflow.
- Browser coverage exercised all seven tabs, probability/log-odds/both modes, score threshold and L2 through range and numeric controls, four built-in datasets, editable classes/features, add/remove, native CSV import, default/imported retraining, live inference, reset, resume, theme, and lesson navigation. Changing the threshold from 60 to 30 changed accuracy from 92.5% to 66.7%; browser diagnostics remained clean.
- Multinomial Logistic Regression replaces the former centroid-logit approximation with a genuine joint softmax optimizer. Decision regions, three class probability surfaces, selected-point probabilities, predictions, per-class coefficients/intercepts, negative log-likelihood, accuracy, and inference all derive from the active fitted model.
- Its 1672×941 reference geometry was matched exactly at the major boundaries: 232px lesson rail, 104px header, 55px tabs, visualization at `(248, 181)` sized `1131×444`, lower inspectors at `(248, 644)` sized `1131×216`, control rail at `x=1391, y=114` sized `265×747`, and the 72px status bar. Tablet and 390px mobile layouts have no horizontal overflow.
- Browser coverage exercised all seven tabs, every feature/model/view control, clickable selected-point inference, four built-in datasets, feature/class editing, add/remove, native CSV import, default/imported retraining, reset, bookmark/share actions, and responsive layouts. Browser diagnostics remained free of warnings and errors.
- KNN Classification retains the existing shared nearest-neighbor engine. Decision regions, sorted neighbor distances, class votes, query confidence, Kth-neighbor radius, metric comparison, and leave-one-out accuracy are all recomputed from the active live dataset.
- Its approved 1672×941 geometry was matched at the major boundaries: the 254px lesson rail, 112px header, 59px tab/data row, feature plot at `(270, 181)` sized `951×474`, controls at `(1235, 181)` sized `415×720`, and four-card diagnostic row at `(270, 669)` sized `951×232`. Tablet and 390px mobile layouts have no document-level horizontal overflow.
- Browser coverage exercised all seven tabs, all query/K/metric/radius controls, plot-click inference, boundary and neighbor-link toggles, four built-in datasets, editable rows, add/remove, native CSV import, lazy-training update, reset, header actions, quick-tip navigation, and responsive layouts. Browser diagnostics remained free of warnings and errors.
- Naive Bayes preserves the shared Gaussian classifier and expands the balanced Iris teaching subsets deterministically to the approved screen's 150 live samples. Priors, Gaussian likelihoods, joint scores, normalized posteriors, decision regions, log probabilities, accuracy, and inference are computed from the active data.
- Its approved 1672×941 geometry was matched exactly at the major boundaries: 275px lesson navigation, 78px header, 45px tabs, action workflow at `(291, 123)` sized `995×588`, diagnostics at `(291, 719)` sized `995×158`, and controls at `(1298, 123)` sized `354×646`. Tablet and 390px mobile layouts have no document-level horizontal overflow.
- Browser coverage exercised all seven tabs, every evidence/model/boundary control, four built-in datasets, editable rows, add/remove, native CSV import, refit/reset/share, and three-class live inference. Browser diagnostics remained free of warnings and errors.
- Decision Tree Classification preserves the shared CART engine. Every tree node, split, class region, confusion cell, precision/recall/F1 value, feature-importance bar, pruning state, and highlighted prediction path derives from the active data and controls.
- Its approved 1672×941 geometry was matched at the major boundaries: 212px lesson navigation, 116px title area, 38px tabs, dataset bar at `(231, 160)` sized `1166×58`, tree/region row at `(231, 226)` sized `1166×451`, diagnostics at `(231, 685)` sized `1166×193`, and 249px control rail at `x=1413`. Tablet and 390px mobile layouts have no document-level horizontal overflow.
- Browser coverage exercised all seven tabs, every tree/pruning/display control, four built-in datasets, editable rows, add/remove, native CSV import, retraining, reset, region-axis selection, and three-class live inference. Browser diagnostics remained free of warnings and errors.
- Random Forest Classification replaces the binary-only workbench wrapper with a reusable multiclass forest engine. Every displayed tree vote, probability, majority decision, OOB prediction/confusion value, feature-importance bar, training score, and decision region is derived from the active fitted ensemble.
- Its approved 1672×941 geometry was matched exactly at the major boundaries: 240px lesson navigation, 120px header, visualization at `(264, 156)` sized `1020×524`, diagnostics at `(264, 686)` sized `1020×200`, dataset summary at `(264, 895)` sized `1020×40`, and controls at `(1304, 127)` sized `348×803`. Tablet and 390px mobile layouts have no document-level horizontal overflow.
- Browser coverage exercised all seven tabs, every forest/sampling control, sqrt/log₂/all feature strategies, bootstrapping and OOB display states, the 500-tree endpoint, per-tree in-bag/OOB counts, four built-in datasets, editable rows, add/remove, native CSV import, retraining, reset, docs/tips actions, and three-class inference. Browser diagnostics remained free of warnings and errors.
- SVM Classification replaces the fixed five-center RBF approximation with a reusable deterministic SMO solver. Dual coefficients, support-vector membership, bias, linear weight vector, margin width, decision scores, predictions, boundary, and all metrics derive from the active data, kernel, and hyperparameters.
- Its approved 1672×941 geometry was matched exactly at the major boundaries: 252px lesson navigation, 132px title area, tab/dataset bar at `(278, 132)` sized `1371×50`, maximum-margin plot at `(278, 192)` sized `972×506`, diagnostics at `(278, 708)` sized `972×187`, control rail at `(1260, 192)` sized `389×703`, and the bottom hint at `y=904`. Tablet and 390px mobile layouts have no document-level horizontal overflow.
- Browser coverage exercised all seven tabs, four kernels, C/gamma/standardization controls, four built-in datasets, editable rows, add/remove, native CSV import, retraining, point selection and plot reset, theme and completion actions, and both-class live inference. Browser diagnostics remained free of warnings and errors; deterministic invariants passed for every kernel.
- Gradient Boosting Classification replaces the generic threshold ensemble with genuine stage-wise logistic boosting. Each CART weak learner fits the current negative log-loss gradient; residuals, sample weights, weighted errors, score distributions, probabilities, log loss, accuracy, staged boundaries, and inference all derive from the active ensemble.
- Its approved 1672×941 geometry was matched exactly at the major boundaries: 222px lesson rail, 88px header, 53px tabs, learner sequence at `(239, 141)` sized `1131×504`, controls at `(1382, 141)` sized `279×524`, four-card diagnostics at `(239, 665)` sized `1422×198`, and dataset footer at `(239, 876)` sized `1422×52`. Tablet and 390px mobile layouts have no document-level horizontal overflow.
- Browser coverage exercised all seven tabs, stage navigation, every learning/depth/subsample/display control, four built-in datasets, editable rows, add/remove, native CSV import, imported-data retraining, reset/full-evolution/header/theme actions, and both-class live inference. Browser diagnostics remained free of warnings and errors; deterministic invariants proved eight-stage loss reduction from 0.598 to 0.273.
- AdaBoost Classification replaces the generic threshold ensemble with genuine weighted decision-stump boosting. Every stump threshold and polarity, weighted error, alpha vote, normalized sample-weight update, staged margin, prediction, and diagnostic derives from the active dataset and controls.
- Its approved 1672×941 geometry was matched exactly at the major boundaries: 248px lesson rail, 96px title area, 42px tabs, visualization at `(260, 203)` sized `1081×369`, controls at `(1354, 82)` sized `309×707`, round sequence at `(260, 582)` sized `1081×218`, and bottom summary at `(260, 810)` sized `1081×121`. Tablet and 390px mobile layouts have no document-level horizontal overflow.
- Browser coverage exercised all seven tabs, round navigation, every learning/display control, four built-in datasets, editable rows, add/remove, native CSV import, default/custom/imported retraining, early stopping, reset/update/header/sidebar/quick actions, and both-class live inference. Browser diagnostics remained free of warnings and errors; deterministic invariants confirmed every round error remained below 0.5, weights stayed normalized, and misclassified samples gained relative weight.
- XGBoost Concept replaces the generic classification workbench with a genuine second-order regression booster. Every displayed gradient, Hessian, candidate threshold, regularized split gain, leaf weight, residual, train/validation curve, feature-importance value, regression metric, and prediction derives from the active fitted ensemble.
- Its approved 1536×1024 geometry was matched at the major boundaries: 230px lesson navigation, content beginning at `x=242`, 70px title area, 34px objective strip, tabs at `(242, 112)` sized `1004×45`, pipeline at `(242, 165)` sized `697×525`, split-gain inspector at `(947, 165)` sized `299×525`, diagnostics at `(242, 697)` sized `1004×305`, and controls at `(1258, 65)` sized `268×951`. The Train action and complete Pipeline Summary remain visible; tablet and 390px mobile layouts have no document-level horizontal overflow.
- Browser coverage exercised all seven tabs, all four built-in datasets, every numeric/range model control, the 200-tree endpoint, metric selector, editable features and targets, add/remove row, native eight-row CSV import, built-in/imported training, reset, theme/share/sidebar/lesson actions, and live regression inference. Browser diagnostics remained free of errors; deterministic invariants showed 20-stage training RMSE reduction from 3.048 to 0.175 with normalized gain importance and identical repeated predictions.
- K-Means Clustering keeps the existing rich numeric and TensorFlow.js MobileNet image-embedding workbench available through Image Embeddings while making the approved point-clustering lab the route's primary view. The shared engine now accepts a seed, making random and K-Means++ initialization reproducible; assignments, mean-updated centroids, inertia, convergence, cluster sizes, history, plot links, and the initializer comparison all derive from real fits.
- Its approved 1536×1024 geometry was matched at the major boundaries: 56px global header, 193px learning rail, title/metrics at `(212, 56)` sized `987×86`, clustering plot at `(212, 142)` sized `987×510`, controls at `(1211, 69)` sized `306×633`, loop panel at `(212, 658)` sized `703×243`, learning-curve region at `(921, 658)` sized `596×243`, convergence summary at `(1259, 667)` sized `250×225`, and tip bar at `(212, 917)` sized `987×41`. Tablet and 390px mobile layouts have no document-level horizontal overflow.
- Browser coverage exercised K increment/decrement, seeded random and K-Means++ initialization, comparison feedback, blobs/rings/mixed datasets, step/run/to-end/autoplay/pause states, speed and seed controls, plot add/remove/reset, all lesson/header/tool actions, and entry/return from the preserved TensorFlow.js advanced workbench. Browser diagnostics remained free of errors; the default live fit reduced inertia from 621.62 to 386.11 while retaining all 12 instructional phases.
- K-Medoids replaces the generic clustering wrapper with a dedicated PAM-style implementation. Medoids are always real dataset rows; initial selection, distance-matrix assignments, exact medoid/non-medoid swap candidates, total dissimilarity, per-step improvements, convergence, cluster colors, distance links, and outlier-cost comparison all derive from the active model. Precomputing pairwise distances reduced metric-control response time to roughly 200 ms without approximating the swap search.
- Its approved 1536×1024 geometry was matched at the major boundaries: 303px lesson rail, 65px top header, lesson content beginning at `x=320`, 35px tabs, plot at `(320, 216)` sized `874×540`, controls at `(1214, 78)` sized `303×676`, diagnostics at `(320, 764)` sized `1197×194`, and the full-width lesson tip at `(320, 968)` sized `1197×46`. Tablet and 390px mobile layouts have no document-level horizontal overflow.
- Browser coverage exercised all seven tabs, all six algorithm phases, K changes, all three distance metrics, both initializers, three built-in datasets, native eight-row CSV import, max-iteration/delay controls, auto-step, play/pause, Back/Next, Run to Completion, Apply Swap, randomization/reset, plot view/scale/distance layers, and every lesson/header/footer action. Browser diagnostics remained free of errors; live step costs decreased from 5,357.18 to 4,758.55 in the default visual sequence, and engine invariants confirmed actual-row medoids, deterministic output, non-increasing costs, and convergence.
- Hierarchical Clustering replaces the generic threshold-grouping wrapper with a reusable agglomerative engine. The full `n-1` merge tree, linkage distances, height cut, optional cluster cap, assignments, dendrogram branches, scatter colors, centroids, pairwise summaries, merge log, silhouette score, and quality indicators all derive from the active data and settings.
- Its approved 1672×941 composition was matched at the major boundaries: 263px lesson rail, 88px header, 46px tab strip, visualization workspace beginning at `(277, 134)` with 501px height, 222px three-card diagnostic row, 315px controls rail beginning at `x=1343`, and the 58px lesson footer. Tablet and 390px mobile layouts were exercised at their target viewport sizes.
- Browser coverage exercised all seven tabs, all four linkage methods, both distance metrics, low/default cuts, the maximum-cluster override, animation and speed states, all three built-in datasets, native eight-row CSV import, dataset/compare actions, and responsive layouts. Browser diagnostics remained free of warnings and errors; invariants confirmed deterministic `n-1` merges, monotone merge heights, complete assignments, and an exact four-cluster cap even below the corresponding cut height.
- DBSCAN replaces the compact two-column demo with the approved lesson workspace while preserving and strengthening the shared density-clustering implementation. Explicit visited state and a deduplicated expansion queue now prevent repeated noise scans and redundant neighbor insertion; all labels, point types, counts, percentages, cluster-size rows, plot colors, and parameter effects come from the live fit.
- Its approved 1672×941 composition was matched at the major boundaries: 241px lesson path, 59px progress/objective header, content beginning at `x=260`, dataset strip and 475px teaching canvas, three-card diagnostics, 351px parameter rail beginning at `x=1304`, quick tip, and bottom mathematical-details strip. Tablet and 390px mobile layouts were verified at their target viewport sizes.
- Browser coverage exercised all seven tabs, ε and minPts default/extreme states, every playback endpoint and direction, play/pause, speed/autoplay, all three built-in datasets, native nine-row CSV import, reset, dataset/header/sidebar/detail actions, and responsive layouts. Browser diagnostics remained free of warnings and errors; ε=0.10 produced 98.7% noise, ε=1.50 produced 96.3% core points, and the approved default resolved to two moon clusters with 5.3% noise.
- Mean Shift replaces the generic fixed-ten-pass wrapper with a reusable kernel mode-seeking engine. Every seed follows a recorded sequence of weighted kernel means; the engine merges nearby terminal modes, assigns points, exposes maximum-displacement history, and reports convergence without requiring a cluster count.
- Its approved 1672×941 composition was matched at the major boundaries: 242px lesson rail, 74px title/progress header, 48px tabs/objective strip, dataset actions, 394px kernel canvas, 74px timeline, 217px three-card diagnostic row, 332px control rail at `x=1323`, practice card, and bottom tip. Tablet and 390px mobile layouts were verified at their target viewport sizes.
- Browser coverage exercised all six tabs, bandwidth 0.5/1.25/5.0, five/default iteration limits, loose/default epsilon, all three kernels, every display toggle, timeline/run/pause/step/reset, three built-in datasets, native nine-row CSV import, roadmap/practice/resource actions, and responsive layouts. Browser diagnostics remained clean; the approved default converged in exactly 12 iterations to four modes with the 45/66/38/51-point split, while bandwidth 0.5 found ten modes and bandwidth 5.0 collapsed them to one.
- Gaussian Mixture Model replaces the generic diagonal-covariance wrapper with a reusable full-covariance EM engine. Every E-step normalizes weighted Gaussian densities; every M-step updates weights, means, and regularized 2×2 covariance matrices; all iteration snapshots, likelihoods, soft memberships, assignments, ellipses, determinants, and summary values are live.
- Its approved 1672×941 composition was matched at the major boundaries: 260px lesson navigation, 94px title/progress header, objective and seven-tab rows, mixture plot from `x=284` to `1104`, EM panel at `x=1113`, dataset panel at `x=1419`, and the three lower diagnostics. Tablet and 390px mobile layouts were verified at their target viewport sizes.
- Browser coverage exercised all seven tabs, projection modes, every EM playback control, EM/Model Controls switching, K=2 and K=6 endpoints, iteration/tolerance/regularization settings, six built-in datasets, native ten-row CSV import, reset, completion/settings/prerequisite actions, and responsive layouts. Browser diagnostics remained clean; engine invariants confirmed monotone likelihood, normalized weights and responsibilities, positive covariance determinants, complete assignments, and deterministic repeated fits.
- Spectral Clustering replaces the generic wrapper with a reusable graph-spectral engine. The kNN affinity graph, normalized operator, orthogonal-iteration eigenvectors, row-normalized embedding, seeded clustering, normalized cut, modularity, silhouette, density, affinity heatmap, and displayed strongest graph edges all derive from the active data and settings.
- Its approved 1672×941 composition was matched at the major boundaries: 235px lesson rail, 62px global header, content beginning at `x=267`, three synchronized pipeline cards, lower result strip, 304px parameter rail, and 31px status footer. Tablet and 390px mobile views were corrected and verified without control or title overlap.
- Browser coverage exercised all seven tabs, all seven built-in datasets, native nine-row CSV import (including the small-matrix/edge case), both kernels and metrics, sigma/neighborhood/seed controls, symmetrization, K stepping, every select and visualization toggle, reset/recompute/download/workspace actions, and responsive layouts. Clean-tab diagnostics remained free of warnings and errors; engine invariants confirmed affinity symmetry, normalized embeddings, sorted eigenvalues, complete k-cluster assignments, and deterministic repeated fits.
- OPTICS replaces the generic clustering wrapper with a reusable priority-ordered density engine. Pairwise distances, bounded neighborhoods, core distances, reachability updates, the complete ordering, threshold extraction, spatial labels, core/border classification, noise counts, and reachability valleys all derive from the active data and parameters.
- Its approved 1672×941 composition was matched at the major boundaries: 188px suite navigation, 84px title area, tab and dataset bars, paired spatial/reachability workspaces, lower cluster summary and quick stats, 338px parameter rail, and fixed status footer. Tablet and 390px mobile layouts were verified without horizontal overflow; the full control rail remains reachable and usable on mobile.
- Browser coverage exercised all seven tabs, all four built-in datasets, native nine-row CSV import, minPts, automatic/custom maximum distance, both metrics, epsilon extraction on/off, highlighting, core/order/log layers, palette, reset/recompute/save/share/export/sidebar actions, and responsive layouts. Browser diagnostics remained clean; deterministic invariants confirmed a unique complete ordering, complete labels/core flags, repeatable reachability values, and exact three-cluster extraction on a controlled fixture.
- Kernel PCA replaces the generic concept card with a reusable centered-kernel eigensolver. Normalized inputs, five Gram-matrix kernels, row/grand-mean centering, deflated eigenvectors, sorted eigenvalues, projected coordinates, component variance, heatmap cells, spectrum bars, and cumulative variance all derive from the active data and parameters.
- Its approved 1672×941 composition was matched at the major boundaries: 218px observatory navigation, 78px title area, seven-tab strip, dataset bar, three synchronized transformation cards, three lower diagnostic cards, 335px parameter inspector, and bottom insight strip. Tablet and 390px mobile layouts were verified without horizontal overflow.
- Browser coverage exercised all seven tabs, all four built-in datasets, native eight-row labeled CSV import, all five kernels, gamma/degree/coefficient controls, centering and normalization, ten-component output, point size/opacity/color, recompute/tour/sidebar/workspace actions, and responsive layouts. Browser diagnostics remained clean; invariants confirmed symmetric zero-mean centered kernels, descending nonnegative eigenvalues, normalized explained variance, correct projection dimensions, and deterministic repeated fits.
- t-SNE replaces the generic concept card with a reusable neighborhood-probability optimizer. Binary-searched perplexity affinities, symmetric probabilities, heavy-tailed low-dimensional affinities, early exaggeration, momentum updates, recentered coordinates, optimization frames, KL divergence, trustworthiness, and continuity all derive from the active high-dimensional samples and settings.
- Its approved 1672×941 composition was matched at the major boundaries: 216px observability navigation, 71px title area, seven-tab strip, paired high-/low-dimensional teaching views, four-stage progress card, four lower data/legend/quality cards, and 367px parameter/KL rail. Tablet and 390px mobile layouts were verified without horizontal overflow.
- Browser coverage exercised all seven tabs, all four built-in datasets, native nine-row high-dimensional CSV import, perplexity/rate/exaggeration controls, 100- and 1,000-iteration states, both metrics and initializers, animation/pause/reset, dataset/header/sidebar actions, and responsive layouts. Browser diagnostics remained clean; deterministic invariants confirmed finite 2D coordinates, multiple snapshots, bounded neighborhood scores, identical repeated fits, and KL reduction from 1.521 to 0.084 on controlled three-cluster data.
- UMAP Concept replaces the generic concept card with a reusable fuzzy-manifold optimizer. Locally scaled kNN memberships, probabilistic fuzzy-union edges, seeded attractive and negative-sampled repulsive updates, recentered/spread coordinates, topology preservation, neighborhood hit, distance correlation, and embedding metadata all derive from active samples and settings.
- Its approved 1672×941 composition was matched at the major boundaries: 216px dataset navigation, 132px lesson header, tab strip, paired high-dimensional graph and low-dimensional manifold cards, three lower diagnostic regions, 283px parameter/metadata/export rail, and bottom learning tip. Tablet and 390px mobile layouts were verified without horizontal overflow.
- Browser coverage exercised all seven tabs, all six built-in datasets, native nine-row CSV import, neighborhood/min-distance controls, all three metrics, random state/spread, density/angular options, reset/recompute/export/header/sidebar actions, and responsive layouts. Browser diagnostics remained clean; invariants confirmed finite deterministic coordinates, valid fuzzy weights, nonempty graph construction, complete output dimensions, and strong controlled-data neighborhood/distance preservation.
- Linear Discriminant Analysis replaces the generic concept card with a reusable supervised projection engine. Standardization, priors, within-/between-class scatter, shrinkage, generalized eigen direction, projected scores, class means, explained separation, leave-one-out predictions, confusion values, and accuracy all derive from active labeled data and settings.
- Its approved 1672×941 composition was matched at the major boundaries: 214px lesson navigation, 79px title bar, seven tabs, 63px dataset/status row, paired original/projection workspace, four lower diagnostic cards, 320px controls/scatter-matrix rail, and bottom equation strip. Tablet and 390px mobile layouts were verified without horizontal overflow.
- Browser coverage exercised all seven tabs, all three built-in datasets, every feature pair, native nine-row labeled CSV import, shrinkage, both prior modes, standardization, both centering modes, reset/run/docs/share/sidebar actions, and responsive layouts. Browser diagnostics remained clean; invariants confirmed a unit direction, symmetric scatter matrices, finite complete outputs, deterministic repeated fits, and exact controlled-data class separation.
- Autoencoder replaces the generic concept card with a real TensorFlow.js encoder-decoder. Adam minimizes reconstruction MSE on the active pixels; optional Gaussian corruption enables denoising; the encoder supplies live latent coordinates, while the decoder produces a genuine seven-step latent traversal. Reconstructions, losses, MSE, PSNR, bottleneck sparsity, parameter count, and model size all derive from the fitted network.
- Its approved 1672×941 composition was matched at the major boundaries: 192px observatory rail, 81px title bar, 51px tab strip, 164px model pipeline, three 377px visualization cards, paired quality/compression diagnostics, 329px control rail, and bottom teaching tip. Responsive tablet/mobile rules collapse both side rails and preserve a single-column teaching flow.
- Browser coverage exercised all seven tabs, all three built-in image datasets, native eight-row square-pixel CSV import, latent/noise/architecture/learning-rate/batch/epoch controls, completed and stopped training, traversal/preset/header/sidebar actions, and a clean fresh-tab run. Engine invariants confirmed finite 4×2 latent output, 4×16 reconstructions, seven decoder frames, three loss epochs, finite MSE, and a positive parameter count; browser diagnostics remained free of warnings and errors.
- Perceptron replaces the compact generic workbench with an approved lesson-scale classifier whose model diagram, linear boundary, playback frames, mistakes, confusion values, and metrics all consume the same active weights, bias, and threshold. The shared engine now accepts deterministic initialization and threshold settings while preserving its existing API defaults.
- Its approved 1672×941 geometry was matched at the major boundaries: 107px lesson rail, 88px header, model panel at `(119, 98)` sized `588×507`, decision plot at `(719, 98)` sized `519×507`, 404px parameter rail, and four lower panels beginning at `y=617`. Tablet and mobile rules collapse the workbench into two and one columns respectively.
- Browser coverage exercised all seven tabs, all three built-in datasets, native ten-row CSV import, weights/bias/threshold, learning rate, epoch limit, all activation functions, immediate and animated training, pause/reset, dataset switching, header/sidebar/explanation actions, and the converged imported case. Clean-tab diagnostics remained free of warnings and errors; invariants confirmed deterministic repeated fits, strict binary predictions for every activation, early convergence, and exact controlled-data classification.
- Multilayer Perceptron replaces the generic concept card with a reusable dense-network engine. Deterministic Xavier weights feed genuine forward activations and binary cross-entropy backpropagation; mini-batch Adam/SGD, optional biases, L2 regularization, a held-out validation split, and configurable one/two-layer topology all affect the learned result.
- Its approved 1672×941 composition was matched at the major boundaries: 220px observatory navigation, 120px header/tab area, network diagram at `(232, 129)` sized `758×451`, 324px activation inspector, 334px model rail, and the three lower decision/curve/performance cards beginning at `y=594`. Tablet and mobile rules collapse the workbench into two and one columns.
- Browser coverage exercised all seven tabs, all three nonlinear datasets, native ten-row CSV import, hidden-layer count, both neuron counts, all hidden activations, Adam/SGD, bias, learning rate, three batch sizes, epoch count, L2, neuron selection, signal controls, train/reset, switch/upload, and sidebar/header actions. A small-import sample-index crash was found and fixed; the final clean tab rendered 17 neurons, 70 learned connections, 480 probability cells, two loss curves, and zero warnings/errors. Deterministic XOR invariants reduced training loss from 0.6353 to 0.0031 and reproduced identical weights and probabilities.
- Neural Network Playground reuses the verified dense MLP engine while adding a configurable validation split and the approved full laboratory shell. Noise regeneration, topology editing, training choices, probability field, loss curves, classification diagnostics, and class-wise output histograms are all live.
- Its approved 1672×941 composition was matched at the major boundaries: 237px playground rail, 99px header/tab area, 103px lesson objective, 162px feature rail, combined network/boundary canvas, 324px model/data inspector, 234px bottom diagnostics, and fixed teaching tip. Tablet and mobile rules collapse the fixed rails into an accessible single-column flow.
- Browser coverage exercised all seven tabs, all three nonlinear datasets, noise/test split/regeneration, feature controls, one/two layers, neuron changes, all activations, bias, Adam/SGD, batch/epoch/rate settings, auto-train, manual train, canvas actions, native ten-row CSV import, and header/sidebar actions. Changing pending topology initially exposed a stale-weight indexing crash; the diagram now derives its shape from the fitted weights. The clean audit rendered 11 trained nodes, 24 weighted edges, 396 probability cells, 20 distribution bins, 100% default accuracy, and zero warnings/errors.
- Convolutional Neural Network preserves the existing TensorFlow.js deep-learning workbench behind the approved layer-inspector shell. The shared CNN now follows the visible two-block architecture: 32/64 Conv2D filters in the primary run, two MaxPool stages, 256 flattened values, a 128-unit dense layer, and a two-class softmax output.
- Its approved dark composition was matched with a fixed 164px suite rail, 64px header, ten-stage pipeline, five live feature-map cards, layer strip, three lower diagnostics, and a fixed inspector rail. The lesson surface now owns its scroll container, while responsive rules collapse the rails and cards for narrower widths.
- Browser coverage exercised every pipeline stage and inspector control, synchronized both kernel editors, verified stride/padding output shapes and filter endpoints, completed primary and advanced TensorFlow runs, reset all computational/display state, captured the trained screen, and repeated training in a fresh tab. The final run reached 100% on the deterministic held-out vertical-bar example with 0.0001 loss and zero browser warnings/errors.
- Convolution Visualizer replaces the generic concept card with a direct matrix laboratory. The shared convolution utility now supports dilation and bias without changing existing call sites, while the page adds four real presets, activation functions, optional normalization, scan-order playback, and native image-to-luminance sampling.
- Its approved 1672×941 composition was matched with a 108px observatory rail, 59px tab header, live input/kernel/output workbench, three lower teaching panels, 316px parameter/data rail, and fixed status footer. The tablet rule moves controls below the workbench without overflow; the mobile rule collapses the experience into a single column.
- Browser coverage exercised all seven tabs, every convolution parameter endpoint, all four presets and activations, bias/normalization, playback and stepping, four sample matrices, PNG upload, invalid 7×7/dilation-3 recovery, full reset, header/sidebar actions, and a clean fresh-tab run. The one-cell output state exposed an evolution-index crash, which is fixed; focused invariants confirmed the original convolution result and the new dilation/bias path.
- Recurrent Neural Network replaces the generic route with the approved unrolled-sequence lesson while preserving the real TensorFlow.js SimpleRNN workbench behind Build / Train. Token vectors, recurrent/input matrices, hidden states, next-token softmax results, state trajectories, information decay, and BPTT gradient norms all come from the same live computation.
- Its approved 1673×940 composition was matched with a 188px suite rail, 60px header/tab region, central sequence and diagnostic workspace, 296px fixed parameter rail, and bottom status strip. At 1024×768 the inspector moves below the lesson without document overflow; narrow-screen CSS provides the single-column form.
- Browser coverage exercised all seven tabs, hidden-size and sequence-length endpoints, every activation and initializer, both bias modes, delay/noise endpoints, all visualization toggles, autoplay/step/reset, three built-in datasets, native ten-token TXT upload, sidebar/header actions, and the preserved advanced TensorFlow run. The clean audit had zero warnings/errors; engine checks confirmed deterministic outputs, exact state/gradient dimensions, bounded probabilities, and orthogonal recurrent rows to machine precision.
- Long Short-Term Memory replaces the generic route with the approved interactive gate-cell laboratory while keeping the route's TensorFlow.js trainer behind Build / Train. The four gates, peephole option, input/recurrent dropout, architecture settings, cell memory, hidden output, equations, timeline, and summaries are one synchronized recurrence.
- Its approved 1673×940 composition was matched with a 180px observatory rail, 51px header, 37px tab strip, 1,061px lesson workspace, 320px inspector, cell-flow diagram, sequence timeline, gate editor, summary cards, and responsive inspector collapse. The 1024×768 tablet view has no document-level horizontal overflow.
- Browser coverage exercised all time navigation, auto-advance and speed, every gate edit, architecture/activation/peephole/dropout settings, three datasets, native numeric CSV import, reset, all seven tabs, navigation actions, and the full 18-epoch TensorFlow.js LSTM run. Two import/timeline edge cases were found and fixed; the clean audit stayed warning/error-free and deterministic recurrence checks matched the displayed equations exactly.
- Gated Recurrent Unit replaces the generic route with the approved gate-flow and RNN-comparison laboratory while preserving its TensorFlow.js GRU trainer behind Build / Train. Token signals, update/reset gates, candidate state, interpolated hidden state, precision, gate history, both trajectories, and retention summaries share one deterministic recurrence.
- Its approved 1673×940 composition was matched at the major boundaries: 188px observatory rail, 75px header, lesson workspace at `x=209`, 48px toolbar, 122px timeline, 409px cell/diagnostics panel, 196px comparison panel, 304px inspector at `x=1356`, and bottom experiment bar. The 1024×768 tablet layout stays within the document width.
- Browser coverage exercised all seven tabs, every numeric endpoint and activation/precision choice, sequence lengths, timeline/step/autoplay/speed, compact/detailed views, three built-in datasets, native labeled CSV import, source behavior, reset, navigation/export/save actions, and the complete 18-epoch TensorFlow.js GRU run. Clean-tab diagnostics stayed empty; invariants confirmed the exact interpolation identity, bounded gates, deterministic output, and real divergence from the comparison RNN.
- Transformer Attention replaces the compact matrix stack with the approved full attention inspector while preserving and extending the route's genuine scaled dot-product logic. Layer/head projections, temperature, causal masking, softmax flow, heatmap, vector slices, and contextual outputs are generated by one reusable engine.
- Its approved 1673×940 composition was matched at the major boundaries: 153px attention rail, 198px token header, flow panel at `(171, 198)` sized `1043×535`, mechanism panel through `y=926`, and 420px inspector at `x=1234`. The inspector moves below the lesson at 1024×768 without document-level overflow.
- Browser coverage exercised all tokens, layer/head endpoints and head stepping, temperature endpoints, causal mask, heatmap row selection, three sequences, reset, observatory actions, and responsive layout. The final clean tab produced 121 live cells, a normalized selected row (99.9% after one-decimal display rounding), and zero warnings/errors; direct invariants confirmed exact row sums, future masking, and weighted value outputs.
- Multi-Head Attention replaces the generic concept wrapper with the approved parallel-head laboratory. Independent Q/K/V projections, head heatmaps, deterministic attention dropout, causal masks, arithmetic aggregate attention, contextual concatenation, output norm, summaries, and comparisons are computed through the shared attention engine.
- Its approved 1673×940 composition was matched at the major boundaries: 227px lesson rail, 63px header and 42px tabs, input strip at `(237, 105)`, four-head workspace at `(237, 223)`, lower diagnostics through `y=842`, 320px inspector, and 90px footer. The inspector moves below the workspace at 1024×768 without document overflow.
- Browser coverage exercised all seven tabs, 128/768 model sizes, 2/4/8 heads, all and individual head views, both dropout endpoints, bias, causal masking, every sample row, native text CSV import, reset, run/save, header/sidebar actions, and responsive layout. A card overlap that intercepted the causal switch was diagnosed and fixed; the clean tab verified upper-triangle zeroing with no warnings/errors.
- Backpropagation Visualizer replaces the generic wrapper with an exact 3→4→2 computational graph. Forward preactivations/activations, MSE loss, output and hidden deltas, both gradient matrices, chain-rule factors, and SGD-updated weights are one synchronized deterministic calculation.
- Its approved 1673×940 composition was matched at the major boundaries: 62px global header, 89px title/control bar, 183px lesson rail, step strip at `(214, 151)`, 428px network canvas, three 255px lower diagnostics, and 309px inspector. The 1024×768 inspector collapse has no document-level horizontal overflow.
- Browser coverage exercised all 12 phases, every transport and playback mode, speed, ReLU/tanh/sigmoid, learning rate, parameter/details tabs, all eight samples, dataset switch, native four-row CSV import, reset/save, seven lesson tabs, and every sidebar action. The clean audit rendered 20 weighted edges and six explicit deltas with zero warnings/errors; finite-difference validation matched the analytic selected gradient to 1.3e-12.
- Few-Shot Learning places the approved episodic prototypical-network visualization in front of the route while preserving the original TensorFlow.js MobileNet feature-extractor, camera, per-class support upload, and live cosine-inference lab behind Build / Train.
- Its approved 1673×940 composition was matched at the major boundaries: 210px observatory rail, 111px header/tabs, title strip at `(228, 111)`, 450px embedding workspace, paired result/prototype row through `y=872`, 290px parameter rail, and fixed tip controls. Tablet layout has no document-level horizontal overflow.
- Browser coverage exercised 2-way/1-shot through 10-way/10-shot, single and batch query modes, Euclidean/cosine metrics, three embedding choices, boundary/distance layers, repeatable new episodes, reset, all seven tabs, all sidebar actions, and entry/return from the preserved MobileNet lab. Clean-tab diagnostics stayed empty; exact class counts, prototype means, nearest-distance predictions, confidence bounds, and deterministic replay were verified.
- Network Builder replaces the route's generic lesson shell with the approved visual CNN architect while preserving the original TensorFlow.js dense builder and trainer behind Train Model and custom-data actions. Sequential shape propagation, same/valid convolution padding, parameter counts, validation, tensor flow, architecture summary, inspector values, and memory estimates all consume the active layer graph.
- Its approved 1673×940 composition was matched at the major boundaries: 210px observatory rail, 124px header/tab area, dataset rail at `(224,139)` sized `244×300`, architecture workspace beginning at `x=484`, and 280px inspector at `x=1378`. The 1024×768 responsive layout has no document-level horizontal overflow.
- Browser coverage exercised valid/invalid/recovered graphs, dataset input changes, add/delete/clear/reset, convolution filters and padding, all overlay/zoom/validation/preview actions, header/sidebar controls, entry/return from the preserved advanced lab, and a completed 45-epoch TensorFlow.js Minimal training run. Diagnostics stayed warning/error-free; direct invariants confirmed exact same/valid shapes, parameter counts, and rejection of a dense layer applied directly to an image tensor.
- Transfer Learning replaces the old simulated scatter/curve demo with the approved experiment console and a deterministic numerical transfer engine. A fixed nonlinear feature extractor feeds a genuinely optimized multiclass softmax head; unfreezing blocks additionally trains feature scales, while all loss, accuracy, top-5, and parameter outputs come from the active run.
- Its approved 1673×940 composition was matched at the major boundaries: 230px experiment rail, 120px header/tabs, main at `x=242`, 738px architecture/comparison area, 316px training controls, 346px dataset-flow rail, and 42px run footer. The 1024×768 layout collapses into one accessible column without document-level overflow.
- Browser coverage exercised all eight tabs, three datasets, three backbones and optimizers, four unfreezing modes, learning rate, batch/epoch/patience settings, early stopping, fine-tune/reset, native six-row CSV upload, docs/data-prep/quick/sidebar actions, and clean fresh-tab rendering. Direct invariants showed deterministic finite 25-epoch history, loss reduction from 2.067 to 1.600, accuracy improvement from 0% to 97.9% on controlled data, and correct frozen/trainable parameter accounting; production build passed.
- Train-Test Split replaces the compact two-card Iris view with the approved partitioning laboratory while reusing the shared seeded splitter. A second held-out split produces real train/validation/test indices; all stream counts, percentages, class bars, field distributions, age histograms, quick stats, and balance feedback consume those indices and active rows.
- Its approved 1673×940 composition was matched at the major boundaries: 205px lesson rail, 136px header/tabs, workspace at `x=227` sized `1027px`, 383px split flow, 174px distribution row, linked insight strip, and 392px control rail at `x=1262`. The 1024×768 layout collapses the controls below the lesson without document-level overflow.
- Browser coverage exercised all eight tabs, 60/25/15 and default ratios, locking, stratification off/on, shuffle state, advanced options, three datasets, native twelve-row CSV import, reset, leakage/insight/header/sidebar actions, and responsive layout. Clean diagnostics stayed empty; direct invariants confirmed exact 70/20/10 controlled counts, complete disjoint coverage, identical class shares, and deterministic repeated splits; production build passed.
- Cross Validation replaces the shared housing-RMSE mini-lab with the approved fold-rotation workspace and a reusable classification CV engine. Every fold trains a nearest-centroid classifier on K-1 partitions, predicts only the held-out indices, and feeds the selected accuracy/precision/recall/F1 metric into the matrix, score distribution, bars, aggregate statistics, and insights.
- Its approved 1673×940 composition was matched at the major boundaries: 246px algorithms rail, 181px title/tab header, main at `(265,193)` sized `1057×668`, 449px fold matrix, 304px controls at `x=1347`, three lower diagnostic cards, and 60px model footer. The 1024×768 layout moves controls below the matrix without document-level overflow.
- Browser coverage exercised K=2 and K=10, all four metrics, stratification/shuffle off and on, seed editing, rotation play/pause/reset, three datasets, native twelve-row CSV upload, all eight tabs, compare/sidebar actions, clean diagnostics, and responsive layout. The clean Iris run produced a real 0.9000 mean with 0.0013 variance; invariants confirmed equal folds, complete once-only validation coverage, train/validation disjointness, bounded scores, and determinism; production build passed.
- Confusion Matrix places the approved five-class evaluation console in front of the route while retaining the original manual binary editor, loan-threshold evaluator, three-class view, formulas, and copy tools at `?advanced=1`. The shared evaluator builds counts from prediction pairs and derives per-class precision, recall, specificity, F1, support, macro metrics, accuracy, and balanced accuracy.
- Its approved 1673×940 composition was matched exactly at the major boundaries: 212px lesson rail, 120px header/tabs, main matrix at `(231,134)` sized `913×576`, gallery at `(231,720)` sized `913×207`, and 502px filter/metric/insight rail at `x=1151`. The 1024×768 layout moves the right rail below the matrix without document-level overflow.
- Browser coverage exercised all 25 matrix cells, true/predicted filters, counts/percentage/normalization modes, class focus, gallery linkage and paging, three datasets, switch/reset, native twelve-row prediction CSV upload, all eight tabs, header/sidebar actions, and entry into both multi-class and loan modes of the preserved lab. Clean diagnostics stayed empty; invariants confirmed exact totals, supports, matrix values, accuracy, balanced metrics, and [0,1] bounds; production build passed.
- ROC–AUC places the approved threshold-analysis lesson in front of the route while preserving the original richer ROC/PR, loan-data, classifier-comparison, threshold, and data-grid laboratory at `?advanced=1`. The shared ROC engine sweeps every distinct score and integrates AUC by the trapezoidal rule; PR points, Youden's J, confusion counts, summary metrics, and score histograms use the same active label/score pairs.
- Its approved 1536×1024 composition was matched at the major boundaries: 202px observatory rail, 122px title/tab header, main workspace beginning at `(223,132)` with a 987px curve/distribution column, and 306px control rail beginning at `x=1210`. The 1024×768 layout moves the threshold controls below the 781px lesson column and has no document-level horizontal overflow.
- Browser coverage exercised both curve modes, threshold endpoints/reset and best-J selection, all five walkthrough cards, three datasets, three model selections, native twelve-row score upload, all eight tabs, sidebar/header actions, and ROC/PR plus classifier-comparison states in the preserved advanced lab. The imported fixture produced AUC 0.92; the clean console had no warnings/errors, and direct invariants confirmed `(0,0)`/`(1,1)` endpoints, monotone FPR/TPR, finite bounded AUC, and conserved confusion totals; production build passed.
- Precision–Recall Curve replaces the small two-card route with the approved imbalanced-classification analysis console while retaining that original numerical lesson at `?advanced=1`. The shared threshold sweep supplies precision and recall at every distinct score; step-integrated AP, confusion metrics, distributions, prevalence baseline, iso-F1 guides, operating point, suggestions, and trend rows all consume the same active score set.
- Its approved 1673×940 composition was matched at the major boundaries: 183px observatory rail, 124px title/tab header, summary at `(210,124)` sized `1164×82`, main analysis row at `(210,216)` sized `1164×445`, 290px controls at `x=1384`, and lower diagnostics at `(210,671)` sized `1444×220`. The 1024×768 layout uses an accessible internal scroll surface with no horizontal overflow; a separate 390×844 audit also remained exactly 390px wide.
- Browser coverage exercised both editable threshold surfaces at 0.01/0.99, all three computed recommendations, all four histogram bin counts, every curve-display toggle, all five trend rows, three built-in datasets, dataset cycling, native eighteen-score CSV import, all eight tabs, export/share/save/reset, positive-class, sidebar/help/profile actions, and the preserved compact lab. The initial recommendation pass was accidentally quadratic over 25,000 rows and was reduced to a linear scan of the already-computed curve; invalid fallbacks, clipped trend content, and tablet scrolling were also fixed. Clean diagnostics stayed empty; fixture invariants confirmed monotone recall, finite bounded precision/AP, exact confusion conservation, and a real AP of 0.6214; production build passed.
- Regression Metrics replaces the compact Recharts lesson with the approved residual-analysis console while retaining the original prediction-error controls, plots, cross-checks, and housing lab at `?advanced=1`. Actual values, model-specific predictions, residuals, four shared metrics, IQR/3σ/5σ sensitivity, linked selections, and insights all consume the same active prediction pairs.
- Its approved 1672×941 composition was matched at the major boundaries: 221px observatory rail, 238px title/tab header, paired plot workspace at `(253,248)` sized `1147×412`, sensitivity panel at `(253,670)` sized `1147×178`, and 238px control rail at `x=1417`. The 1024×768 view keeps the lesson in a vertical internal scroll surface with no horizontal overflow, and the 390×844 audit remained exactly 390px wide.
- Browser coverage exercised all eight tabs, three datasets, three models, every display toggle and outlier rule, linked point selection, all five plot tools, model comparison, native twelve-row actual/predicted upload, sidebar/header actions, and the preserved original lab. Clean diagnostics stayed empty; direct checks confirmed residual identity, MSE = RMSE², finite metrics, perfect-prediction MAE/MSE/RMSE of zero and R² of one, while the uploaded fixture recomputed MAE 0.6083, MSE 0.4008, RMSE 0.6331, and R² 0.9966; production build passed.
- Bias–Variance Tradeoff replaces the shared mini-lab with the approved three-view explorer while preserving that original polynomial lesson at `?advanced=1`. A deterministic seeded split, real ridge-stabilized polynomial/spline fitting, a full degree sweep, thirteen bootstrap refits, uncertainty bands, and all live metrics consume the same active observations.
- Its approved 1536×1024 composition was matched at the major boundaries: 202px observatory rail, 147px title/tab header, explorer at `(214,203)` sized `1032×641`, 154px live-metric strip, and 278px control rail beginning at `x=1258`. The 1024×768 layout moves all controls below the 2,802px lesson surface with exact contained width; the separate 390×844 audit also remained exactly 390px wide.
- Browser coverage exercised all eight tabs, degree animation, three model kinds, three datasets and cycling, feature scaling, seed, noise/test-size endpoints, completion, collapse, reset, native sixteen-row x/y upload, every sidebar action, and the preserved original lab. A control-rail overlap that hid Compare/Explain was fixed. Clean diagnostics stayed empty; degree 1/5/15 produced finite underfit/balanced/overfit states, with validation MSE rising from 0.0089 at degree 5 to 0.0517 and average bootstrap spread from 0.044 to 0.825 at degree 15; production build passed.
- Missing Values places the approved data-quality dashboard in front of the route while preserving the original multi-strategy imputation, distribution-overlay, column-statistics, and before/after workbench at `?advanced=1`. Table cells, missing-only filtering, per-column bars, donut totals, heatmap, insights, five preview methods, indicators, and mean-clean export all consume the active rows.
- Its approved 1672×941 composition was matched exactly at the major boundaries: 198px learning rail, 113px title header, 37px tab band, table at `(212,160)` sized `1066×382`, lower analysis at `(212,550)` sized `1066×240`, insights at `(212,798)` sized `1066×126`, and 386px right rail. The 1024×768 view moves the right rail below the 1,230px lesson surface without horizontal overflow, and the 390×844 audit remained exactly 390px wide.
- Browser coverage exercised all eight tabs, table/compact views, missing-only filtering, search, row-size paging and page transitions, three datasets, indicators, heatmap/settings/configuration actions, method comparison, clean export, native twelve-row mixed CSV import, every sidebar action, and a real mean-imputation run in the preserved lab. The imported fixture produced six missing cells out of 60 and distinct data-derived previews; exported built-in data contained exactly 75 rows, 10 columns, and zero blank cells. Clean diagnostics, focused ESLint, TypeScript, route/navigation audits, and production build all passed.
- Scaling & Normalization places the approved full feature-scaling laboratory in front of the route while preserving the original distribution, all-method comparison, feature statistics, formulas, and method guide at `?advanced=1`. Min-Max, Z-score, and robust scaling, paired distributions, geometry scatters, feature controls, and summaries all consume the same active numeric columns.
- Its approved 1672×941 composition was matched at the major boundaries: 270px observatory rail, 143px title/tab/dataset header, 1,128px four-feature workspace, 274px method/feature rail, and 55px teaching footer. The 1024×768 layout moves controls below the lesson without horizontal overflow, and the separate 390×844 capture remains a single-column mobile flow.
- Browser coverage exercised all eight tabs, three methods, three datasets, all/custom selection, feature search, individual exclusion, every visualization option, reset, plot expansion, header/sidebar/footer actions, native twelve-row numeric CSV import, and the preserved original lab's Z-score/statistics states. Clean diagnostics stayed empty; invariants confirmed Min-Max `[0,1]`, Z-score mean 0/std 1, robust median 0/IQR 1, and safe zero output for constant columns; focused ESLint, TypeScript, route/navigation audits, and production build passed.
- Categorical Encoding replaces the compact six-row table with the approved end-to-end encoding console while preserving that original label/ordinal/frequency/one-hot cross-check at `?advanced=1`. One-hot, ordinal, frequency, and K-fold target encoding now share the active categorical rows, target, cardinality scan, leakage guardrails, comparison metrics, and export.
- Its approved 1672×941 composition was matched at the major boundaries: 181px suite navigation, 214px objective/progress rail, 126px title/tab header, 952px raw/encoded/diagnostic workspace, 327px transform rail, and 62px experiment footer. The 1024×768 layout moves the lesson and controls into the document flow without horizontal overflow, and the 390×844 audit remains exactly 390px wide.
- Browser coverage exercised all eight tabs, four methods, every slider endpoint, three built-in datasets, target selection, output-card linkage, apply/reset, export, native twelve-row CSV import, header/sidebar/footer actions, and the preserved original workbench's risk encoding. Clean diagnostics stayed empty; reusable-engine checks confirmed finite bounded deterministic target values, one-hot row sums of one, valid ordinal/frequency ranges, and that changing a held-out target cannot alter its own fold encoding; focused ESLint, TypeScript, route/navigation audits, and production build passed.
- Outlier Detection replaces the compact 22-value scatter with the approved anomaly-analysis console while preserving that original Z-score/IQR workbench at `?advanced=1`. Reusable IQR and Z-score detection assigns normal, mild, and severe bands; IQR capping, row removal, quartiles, fences, density bins, impact statistics, and all three visualizations consume the same active values.
- Its approved 1672×941 composition was matched at the major boundaries: 196px observatory rail, 73px header, 90px objective/progress strip, workbench at `(207,163)` sized `1194×734`, 245px controls at `x=1411`, and 44px dataset footer. The 1024×768 layout moves the inspector below a contained two-column visualization, and the 390×844 audit remains exactly 390px wide.
- Browser coverage exercised all seven tabs, IQR/Z-score, multiple features, multiplier endpoints and steppers, three datasets, inspect/cap/remove/reset, scatter tools, version toggle, native twelve-row CSV import, header/sidebar/transform actions, and the preserved original IQR state. The imported fixture identified its two injected extremes; clean diagnostics stayed empty, and reusable-engine checks confirmed finite deterministic scores, correct severe-IQR/mild-Z classification, and fence capping from 100 to 20.5; focused ESLint, TypeScript, route/navigation audits, and production build passed.
- Feature Selection replaces the generic mini-lab with the approved ranking-and-correlation workspace while preserving that original housing top-k regression lesson at `?advanced=1`. Variance, Pearson/point-biserial correlation, importance ranking, feature selection, pairwise network edges, validation curves, and recommendations all consume the same active numeric rows.
- Its approved 1536×1024 composition was matched at the major boundaries: 206px observatory rail, 72px header, 49px tabs, 83px objective/progress strip, 390px ranking at `(220,204)`, 515px network at `(619,204)`, 392px controls at `x=1144`, and lower insights/activity through `y=978`. The 1024×768 lesson uses an internal vertical scroll surface with no horizontal overflow, and the 390×844 audit remains exactly 390px wide.
- Browser coverage exercised all eight tabs, three ranking paths, individual/bulk/manual/automatic/threshold selection, all three datasets, correlation tools, validation/reset/save, every insight/activity/header/sidebar action, collapse/restore, native twelve-row numeric CSV import, and representative controls in the preserved original lab. Testing exposed and fixed leaked source targets in the alternate datasets, a global `.collapse` visibility collision, selector/rank desynchronization, a negative zero-selection marker, and responsive clipping. Clean diagnostics stayed empty; direct invariants confirmed correlation symmetry/identity, target exclusion, descending ranks, bounded monotonic performance, focused ESLint, TypeScript, and route/navigation audits.
- Polynomial Features replaces the shared one-variable mini-lab with the approved multivariate transformation console while preserving that original fitted-curve lesson at `?advanced=1`. A reusable monomial generator enumerates powers and interactions; standardization, design-matrix construction, ridge-stabilized least squares, predictions, equations, plots, and metrics all consume the same active rows.
- Its approved 1673×940 composition was matched at the major boundaries: 238px observatory rail, 155px header, input at `(260,155)` sized `207×335`, expanded terms at `(483,155)` sized `344×335`, matrix at `(843,155)` sized `498×335`, comparison at `(260,503)` sized `843×275`, example at `(1115,503)` sized `225×275`, 300px settings rail at `x=1352`, and lower insight strip at `(260,792)` sized `1392×127`. The 1024×768 lesson scrolls internally without horizontal overflow, and the 390×844 audit remains exactly 390px wide.
- Browser coverage exercised all eight tabs, degrees 1–5, individual/add feature paths, bias and interaction toggles, raw/standardized values, three model types, both plot modes, all examples, three datasets, result/header/sidebar/collapse actions, native twelve-row numeric CSV import, and representative controls in the preserved original lab. Clean diagnostics stayed empty; direct invariants confirmed exact term counts, matrix shape, zero standardized means, finite deterministic fits, and a degree-two near-exact recovery that improves on degree one; focused ESLint, TypeScript, and route/navigation audits passed.
- Moving Average replaces the compact monthly-sales chart with the approved hourly signal laboratory while preserving that original rolling-window forecast at `?advanced=1`. A reusable engine now owns trailing and centered fixed-width averages, signed lag shifting, residuals, absolute residuals, and every chart/insight readout.
- Its approved 1536×1024 composition was matched at the major boundaries: 253px observatory rail, 62px breadcrumb header, 120px objective/title strip, 48px lesson tabs, 61px dataset/range toolbar, raw chart at `(267,291)` sized `987×244`, paired residual charts through `y=809`, 282px controls at `x=1254`, insight row at `y=808`, and footer at `y=967`. The 1024×768 view uses an internal vertical scroll surface without horizontal overflow, and the 390×844 audit remains exactly 390px wide.
- Browser coverage exercised all seven tabs, five time ranges, window endpoints/default, both alignments, lag endpoints/default, all four display layers, five themes, three datasets, native sixteen-point CSV import, export/save/calendar/expand/reset, every sidebar action, collapse/restore, and the preserved original route. Testing fixed global utility-class collisions, incorrect sidebar auto-placement, and shortened centered windows at the series tail. Clean diagnostics stayed empty; direct invariants confirmed exact trailing/centered values, signed lag behavior, residual identity, absolute residuals, finiteness, and determinism; focused ESLint, TypeScript, and route/navigation audits passed.
- Exponential Smoothing replaces the compact monthly-sales chart with the approved forecast-and-uncertainty console while preserving that original alpha-weighted forecast at `?advanced=1`. A reusable engine owns the recursive level, one-step forecast/errors, constant h-step forecast, confidence bounds, MAE, RMSE, MAPE, bias, and Theil's U.
- Its approved 1673×940 composition was matched at the major boundaries: 198px observatory rail, 62px utility header, 131px title/objective/tabs area, forecast and error workspace at `(222,193)` sized `1098×527`, 322px model controls at `(1333,193)`, and four lower diagnostic cards from `(222,752)` through `y=923`. The 1024×768 view separates its 527px forecast and 527px control tracks in an internal scroll surface without horizontal overflow, and the 390×844 audit remains exactly 390px wide.
- Browser coverage exercised all eight tabs, alpha/horizon endpoints and defaults, three confidence levels, both toggles, control collapse/reopen, three datasets and cycling, native sixteen-observation CSV import, export/theme/learn-more, every sidebar action, collapse/restore, and the preserved original route. Testing fixed numeric confidence-option synchronization and a responsive auto-track overlap. Clean diagnostics stayed empty; direct invariants confirmed exact recurrence and one-step identity, horizon length, symmetric widening intervals, finite metrics, bounded-alpha behavior, and determinism; focused ESLint, TypeScript, and route/navigation audits passed.
- Holt–Winters replaces the compact generic lesson with the approved four-part decomposition and forecast console while preserving the original full time-series workbench at `?advanced=1`. A reusable engine now owns additive and multiplicative level, trend, seasonal, fitted, residual, forecast, MAE, RMSE, MAPE, and sMAPE outputs.
- Its approved 1536×1004 composition was matched at the major boundaries: 240px observatory rail, 279px title/objective/pipeline region, decomposition at `(256,279)` sized `983×484`, 285px controls at `(1251,279)`, lower diagnostics from `y=763` through `y=961`, and fixed status footer. The 1024×768 lesson uses an internal vertical scroll surface without horizontal overflow, and the 390×844 capture remains exactly 390px wide.
- Browser coverage exercised all eight tabs, five zoom ranges, additive/multiplicative modes, seasonal-length changes, every alpha/beta/gamma endpoint, auto-optimization, forecast-horizon minimum/default, three datasets and cycling, forecast/calendar actions, native 28-observation CSV import, every sidebar action, collapse/restore, and the preserved original route. Clean diagnostics stayed empty; direct invariants confirmed series dimensions, finite additive/multiplicative results, nonnegative metrics, seasonal forecast spread, and determinism; focused ESLint, TypeScript, route/navigation audits, and production build passed.
- ARIMA Concept replaces the compact synthetic chart stack with the approved stationarity and order-analysis observatory while preserving that original lesson at `?advanced=1`. A reusable numerical engine now owns repeated differencing, ACF, PACF, ridge-stabilized AR/MA fitting, iterative innovations, forecast restoration, uncertainty, log likelihood, AIC, and BIC.
- Its approved 1672×941 composition was matched at the major boundaries: 180px observatory rail, 128px title/objective/tab header, 1,160px analysis workspace, 332px model-control rail, dataset/meta strip at `y=142`, time-series/decomposition pair, ACF/PACF row, forecast/residual row, and four insight cards through `y=917`. The 1024×768 and 390×844 layouts stay exactly within their viewport widths using an accessible internal scroll surface.
- Browser coverage exercised all eight tabs, p/d/q and P/D/Q/S endpoints, every differencing state and arrow boundary, horizon endpoints/default, all three confidence intervals, run/auto-suggest, three datasets, native eighteen-observation CSV import, inspect/expand, every sidebar/header action, collapse/restore, and the preserved original lab. A second-difference forecast explosion was found and corrected by keeping regular differencing distinct from seasonal residual offsets. Clean diagnostics stayed empty; direct invariants confirmed differencing lengths, ACF/PACF lag-zero identities, forecast and interval shapes, finite ordered bounds, order sensitivity, and determinism; focused ESLint, TypeScript, route/navigation audits, and production build passed.
- Time-Series Anomaly Detection replaces the generic rolling-baseline chart with the approved live observability console while preserving that original lab at `?advanced=1`. A reusable robust detector now owns centered rolling medians, MAD-scaled thresholds, contextual-change scores, normal/medium/high classes, and selected-anomaly detail.
- Its approved 1536×1024 composition was matched at the major boundaries: 214px observatory rail, 70px dataset/stream header, 128px lesson/progress band, monitoring workspace from `(214,198)` through `y=956`, 248px detection rail, three diagnostic rows, and 68px model-status footer. The 1024×768 and 390×844 layouts stay exactly within their viewport widths using the page's internal scroll surface.
- Browser coverage exercised all eight tabs, three datasets and window sizes, live pause/resume, three timezones, five chart tools, all three model labels, sensitivity endpoints/default, three cooldowns, both switches, 19 linked markers, selected detail, recalculate/resume/view-all/explain actions, native twenty-point CSV import, all navigation/header actions, collapse/restore, and the preserved original lab. Endpoint false positives and a compact switch overlap were corrected. Clean diagnostics stayed empty; direct invariants confirmed result shape, finite values, ordered bands, injected-spike detection, high-severity classification, sensitivity monotonicity, and determinism; focused ESLint, TypeScript, route/navigation audits, and production build passed.
- RNN Forecasting replaces the general-purpose page shell with the approved recurrent forecast observatory while preserving the original TensorFlow.js SimpleRNN trainer at `?advanced=1`. A reusable deterministic recurrent engine supplies distinct vanilla, gated LSTM, and GRU state transitions, recursive seasonal predictions, ordered uncertainty bands, and synchronized diagnostics.
- Its approved 1536×1024 composition was matched at the major boundaries: 208px observatory rail, 151px breadcrumb/title/tab header, objective and model overview through `y=447`, rolling forecast through `y=638`, three diagnostics through `y=852`, lower summary/insight/action row, and 244px forecast/model rail. The 1024×768 and 390×844 audits remain exactly within their viewport widths.
- Browser coverage exercised all eight tabs, three datasets, all numeric endpoints/defaults, three targets, three scalers, three recurrent model variants, forecast generation, native 32-observation CSV upload, architecture and quick actions, every navigation/header action, collapse/restore, and the preserved TensorFlow.js training route. Testing corrected clipped header and generate controls, lower-row alignment, and inactive step-size semantics. Clean diagnostics stayed empty; direct invariants confirmed state/horizon shapes, finiteness, ordered bounds, deterministic replay, model-kind differences, and input sensitivity; focused ESLint, TypeScript, route/navigation audits, and production build passed.
- LSTM Forecasting replaces the shared trainer shell with the approved memory-gate lesson while keeping the full TensorFlow.js LSTM path at `?advanced=1`. Its computed forget, input, candidate, output, cell, hidden, forecast, and interval data drive every central diagram and diagnostic.
- The 1672×941 layout matches the reference's 214px learning rail, objective/progress and dataset header, three-stage sequence-to-cell-to-forecast workspace, gate/cell/hidden row, four insights, and 330px controls. All eight tabs, endpoint parameters, three datasets, three views, layer/unit/rate choices, visual toggles, native upload, run/navigation/collapse actions, exact responsive widths, and completed TensorFlow.js training were verified without warnings.
- GRU Forecasting uses its own approved experiment dashboard and the same upgraded recurrent engine's genuine update/reset gate recurrence. Rolling forecast, ordered interval, 360 live heat cells, three gate-mechanic summaries, metrics, comparison and error display stay synchronized with active data and configuration.
- Its 1672×941 capture matches the 218px modeling rail, 96px title/tab row, 60px run toolbar, six-panel analytical workspace, and 306px configuration inspector. Browser coverage exercised the full dataset/run/control/select/toggle/action matrix, native upload, collapse, exact 1024×768 and 390×844 containment, and a completed TensorFlow.js GRU run; direct invariants and the final production build passed.
- A suite-wide mathematical-correctness pass hardened the shared statistics, metrics, regression, classification, clustering, dimensionality, evaluation, preprocessing, neural, and forecasting engines. Corrections include MSE-consistent backpropagation gradients, final-parameter GMM responsibilities and covariance regularization, RKHS SVM margins, objective-consistent SVR gradients, true macro-F1 cross-validation, standard rank-based trustworthiness/continuity, standard normalized cut, non-inflated partial Kernel-PCA explained variance, degree-zero polynomial features, zero-variance coordinate descent, and exact SES forecast variance/MAPE handling.
- Page-local studio math was also corrected: Stacking now learns ensemble weights/meta-features on a validation partition instead of the test set, user-based collaborative filtering uses only co-rated non-target items, matrix-factorization RMSE matches the raw objective optimized by SGD, and AdaBoost no longer fabricates a nonzero learner weight at chance error. The expanded production-backed suite passes 24 tests; ESLint, TypeScript, the 821-route build audits, production build, 137-route HTTP smoke test, and four responsive browser tests all pass.
- The five platform entry screens now use a shared approved dark observatory shell while retaining each original route implementation behind `?advanced=1`. Exact-size and mobile captures cover Home, Documentation, Dataset Library, Sitemap, and Implementation Matrix; semantic browser tests exercised representative navigation, content, filter, dataset, upload, and status paths. Screenshot review exposed and resolved the Sitemap/Matrix root-class collision before completion.
- Q-Learning Grid World now opens directly into the approved 7×6 reinforcement-learning laboratory while preserving the original complete lesson at `?advanced=1`. The shared engine gained optional seeded randomness without changing existing callers. Browser testing edited reward cells, trained the real Bellman update loop, verified nonzero Q-values and policy arrows, switched result tabs, checked the advanced route, and confirmed exact desktop/mobile containment.
